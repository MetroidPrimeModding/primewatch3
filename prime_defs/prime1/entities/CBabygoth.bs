struct CBabygoth: CPatterned {
  // `GetCollisionPrimitive` override — returns `&x930_aabox` instead of the
  // base `CPhysicsActor` box; `Box()` is refreshed from the bounding box.
  CAABBPrimitive collisionAABox 0x930
}
