# P6.1 — archived completed task

Archived completed task. Carried by the commit that adds this file (`port(P6.1): …` on
`rust-conversion`). Summary lives in [`TASKS.md`](../TASKS.md); this file is the full entry.

---

- [x] **P6.1** Walk `CObjectList` off `g_stateManager` into `HashMap<TUniqueID, GameInstance>`,
  refreshed once per frame. — `DONE`

  **Port from:**
  - `../primewatch2/src/utils/GameObjectUtils.cpp:26-61:getAllObjects` — read `g_stateManager["allObjects"]`
    (auto-derefs `*CObjectList`), then `firstID` / `size` (`read_u16`, clamp `size` to 1024). Take the
    `list` member (`SObjectListEntry[1024]`), and walk the intrusive linked list: starting at
    `currentId = firstID`, while `currentId != 0xFFFF`, index element `currentId` of `list`
    (stride = `SObjectListEntry.size` = 0x8), read its `entity` (`*CEntity` auto-deref), read
    `entity["vtable"].read_u32()`, and if `MP1_VTABLES` maps that vtable **and** the mapped name is a
    known struct, retype the entity instance to it; store `entity` under `currentId`; advance
    `currentId = currentEntry["next"].read_u16()`. `count > size` is the emergency break.
  - `../primewatch2/src/utils/GameObjectUtils.cpp:13-24:getObjectByEntityID` — same list, single
    lookup: `actualId = eid & 0x3FF`, element `actualId` of `list`, return its `entity` (no vtable
    retype). Needed by `WorldRenderer.cpp:143` (camera lookup) — port it now alongside `get_all_objects`.
  - `../primewatch2/src/PrimeWatch.cpp:483-488:doMemoryParse` — `entities = getAllObjects()` runs once
    per frame, right after `updateFromDolphin`, gated on `GameDefinitions::isLoaded()`.
  - Skip for now (later phases): `getAllCObjectReferences` / `getAllLoadingDatas` (resource browser,
    P7/P8), `objectTagToString` / `fourCCToString` (renderer helpers, P7).

  **Steps:**
  - [x] 1. New `src/mem/game_object_utils.rs` (`pub mod game_object_utils;` in `src/mem/mod.rs`). Ports
     `utils/GameObjectUtils.cpp`. `pub type TUniqueID = u16;`.
  - [x] 2. `pub fn get_all_objects(ctx: &Ctx) -> std::collections::HashMap<TUniqueID, GameInstance>`.
     Resolve `get_state_manager().get_member(ctx, "allObjects")`; on `None` (or `None` on `firstID` /
     `size` / `list`) return an empty map. Clamp `size` to 1024. Loop as in the port spec; use
     `list.element(ctx, current_id as u32)` for the stride arithmetic (its `element_size` is the
     `SObjectListEntry` struct size, 0x8). Retype: `if let Some(name) = vtable_class_name(vtable)` +
     `ctx.structs.get_struct_by_name(name).is_some()` → `entity.type_name = name.to_string()`.
  - [x] 3. `pub fn get_object_by_entity_id(ctx: &Ctx, eid: u16) -> Option<GameInstance>` — `eid & 0x3FF`,
     `list.element(ctx, actual_id as u32).get_member(ctx, "entity")`.
  - [x] 4. Wire per-frame: add `objects: HashMap<TUniqueID, GameInstance>` to `App` (`src/app.rs`),
     populated in the `RedrawRequested` arm immediately after `self.mem.update_from_dolphin(...)`,
     inside the existing `if self.defs_loaded` block, via
     `Ctx::new(&self.structs, &self.mem)`. Replace the `// TODO(P9.1): the entities parse` comment
     with the real call (keep a trimmed note that P9.1 still owns input→parse→ui→render ordering).
     `self.objects` is unused until P7 — `#[allow(dead_code)]` it (like the pre-existing note on
     `structs`; `structs` is now read here so drop its `#[allow(dead_code)]`).
  - [x] 5. Tests in `game_object_utils.rs` (`#[cfg(test)]`): skip-if-absent `mem1.raw` loader + real
     `prime_defs` load via `concat!(env!("CARGO_MANIFEST_DIR"), "/prime_defs")`. With the dump:
     `get_all_objects` returns a non-empty map, every value's `address` masks into RAM
     (`addr & 0xFF00_0000 == 0x8000_0000`), at least one entity got retyped away from `"CEntity"`,
     and `get_object_by_entity_id` for a key from the map resolves to the same address. Zeroed-memory
     sanity: `get_all_objects` does not panic.

  **Watch for:**
  - `GameInstance` has no mutable `offset` — the C++ `entry.offset += type.size * id` becomes
    `list.element(ctx, id)`. Confirm `SObjectListEntry` resolves as a struct so `element_size` is 0x8,
    not the `primitive_size` fallback of 4.
  - Read the `next` link from the **indexed** `currentEntry`, not the base `list` handle (C++ bug-parity:
    it reads `currentEntry["next"]`, and `prev`/`next` are per-slot).
  - `firstID`/`size`/`next` are `u16`; the sentinel is `0xFFFF`. Keep `currentId` a `u16` so the
    compare is exact.
  - Established convention (P4.2 / P5.1): reads are `Option`, no default substitution. A mid-walk
    `None` on a structural read → stop the walk (don't unwrap, don't fabricate). Document any spot
    where this diverges from C++'s total-read behavior.
  - Retype only when **both** conditions hold (`GameVtables.cpp` parity, echoed in the P5.2 note):
    vtable is in `MP1_VTABLES` and the mapped name is a real `.bs` struct.
  - `HashMap` import; `rustfmt` 2-space indent.

  **Done when:** `cargo clippy --all-targets` + `cargo test` clean; `get_all_objects` /
  `get_object_by_entity_id` ported with the tests above; `App` refreshes `self.objects` once per
  frame behind the `defs_loaded` gate.

  **Implementation notes (port-implementer):**
  - New `src/mem/game_object_utils.rs` (`pub mod` in `src/mem/mod.rs`): `pub type TUniqueID = u16`,
    `get_all_objects(&Ctx) -> HashMap<TUniqueID, GameInstance>`, `get_object_by_entity_id(&Ctx, u16)
    -> Option<GameInstance>`. Ports `GameObjectUtils.cpp:13-24` + `26-61` only; the resource-browser
    / string helpers are left for P7/P8 as the task says.
  - `entry.offset += type.size * id` → `list.element(ctx, id)`. Confirmed `SObjectListEntry`
    resolves as a struct (test `object_list_entry_resolves_as_struct_with_stride_8`) so
    `element_size` is 0x8. `next` is read from the indexed `current_entry`, not the base `list`
    (C++ bug-parity). `current_id` kept `u16`; sentinel `0xFFFF` as `const LIST_END`. Emergency
    break `count > size` with `size` clamped to 1024 (`u16::min(1024)`).
  - Retype condition folded into one `if let` via
    `vtable_class_name(vtable).filter(|n| ctx.structs.get_struct_by_name(n).is_some())` — both
    conditions required, no new `collapsible_if` lint.
  - **Deviation from C++ (sanctioned by P4.2/P5.1 convention, noted in doc-comment):** C++ reads are
    total (OOB → 0). Here a `None` on any structural/value read mid-walk (`firstID`/`size`/`list` →
    empty map; `entity`/`vtable`/`next` → stop, return what's gathered). Never triggers on a valid
    snapshot.
  - **Deviation from the task's step-5 assertion:** `addr & 0xFF00_0000 == 0x8000_0000` is too tight
    — MEM1 spans `0x80000000..0x81800000` and real entities sit above `0x81000000`, so the test
    asserts `addr & 0x8000_0000 == 0x8000_0000` plus `ctx.mem.read_u32(addr).is_some()` (readable in
    the snapshot) instead. Reviewer: confirm this is acceptable.
  - `app.rs`: added `objects: HashMap<TUniqueID, GameInstance>` field (`#[allow(dead_code)]`, note
    updated), populated in the `RedrawRequested` arm right after `update_from_dolphin`, inside the
    `if self.defs_loaded` block, via `Ctx::new(&self.structs, &self.mem)`. Dropped
    `#[allow(dead_code)]` from `structs` (now read there). Trimmed the `// TODO(P9.1)` comment.
  - `get_object_by_entity_id` carries an expected dead-code warning until P8 wires the
    `WorldRenderer` camera lookup — consistent with `get_main`/`get_memory_card` in `globals.rs`.
  - `cargo clippy --all-targets`: clean (no new lints in the touched files; pre-existing
    `collapsible_if` in `prime_structs.rs` and the defs-layer dead-code warnings unchanged).
  - `cargo test`: 27 passed, 0 failed (was 24; +3 in `game_object_utils`; the mem1.raw tests run in
    this env and pass).

  **Review:** Walked `get_all_objects` / `get_object_by_entity_id` side by side with
  `GameObjectUtils.cpp:13-61`: `allObjects` auto-deref, `firstID`/`size` `read_u16` + 1024 clamp,
  `list` member, per-slot `list.element(ctx, id)` stride arithmetic (struct size 0x8 — confirmed by
  `object_list_entry_resolves_as_struct_with_stride_8`), `entity` auto-deref, `vtable` `read_u32`,
  retype gated on `vtable_class_name(v)` AND `get_struct_by_name(name).is_some()` (matches C++
  `MP1_VTABLES.count(v) && structByName(name).has_value()`, incl. the `0x803d9ce0` alias which has
  no struct so never retypes), insert-before-advance, `next` read from the indexed `current_entry`
  (C++ per-slot bug-parity), `count > size` pre-increment emergency break, `0xFFFF` sentinel with
  `current_id: u16`. `eid & 0x3FF` and no-retype on the id path match. BE conversion stays solely in
  `GameMemory`; addressing masked in `address_to_offset`; no new globals; `Ctx` threaded explicitly.
  Per-frame wiring sits inside `if self.defs_loaded` right after `update_from_dolphin`. Both flagged
  deviations (mid-walk `None` stops the walk; test mask relaxed to `& 0x8000_0000` + readability
  check) are sound — the old `& 0xFF00_0000` assertion was simply wrong for entities above
  `0x81000000`. `cargo fmt --check` clean; `cargo clippy --all-targets` carries only pre-existing
  warnings (`area_utils.rs` unused imports slated for P6.2, defs-layer dead code, expected
  `get_object_by_entity_id` dead-code until P8); `cargo test` 27/27 including the live-dump walk.
