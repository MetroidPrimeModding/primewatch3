# P3.1 — archived completed task

Archived from `TASKS.md` on completion. Commit: `5004acd`. See `TASKS.md` for the one-paragraph summary that replaced this.

---

- [x] **P3.1** Round out `src/mem/game_memory.rs` read surface: `read_bool`, `read_string`,
  bitfield-masked reads; fix `read_u16/u32/...` slicing bugs (`self.data[offset..]` needs a bounded
  slice before `try_into`). — `DONE`

  **Port from:**
  - `../primewatch2/src/GameMemory.cpp:37-69` — `read_u8` / `read_u16` / `read_u32` / `read_u64` /
    `read_float` / `read_double`: BE conversion (`MemoryAccess::beToHost*`), `memcpy` from
    `memory.data() + getRealPtr(address)`.
  - `../primewatch2/src/GameMemory.cpp:29-35` — `getRealPtr`: `address & 0x7FFFFFFFu`, returns `0` if
    `masked > DOLPHIN_MEMORY_SIZE` (Rust diverges here — see Watch for).
  - `../primewatch2/src/GameMemory.h:14-19` — the canonical `GameMemory` C++ read surface (no
    bool/string/bitfield; those live on `GameMember`).
  - `../primewatch2/src/defs/GameDefinitions.cpp:234-243` — `getBits<T>(v, bit, length)`: the bitfield
    mask/shift primitive this task pushes down into `game_memory.rs` so P4.2 `GameInstance` can reuse
    it. Semantics: `bit = bitOptional.value_or(0)`, `length = lengthOptional.value_or(0)`,
    `mask = length == 0 ? ~0 : (1ULL << length) - 1`, result `(v >> bit) & mask`.
  - `../primewatch2/src/defs/GameDefinitions.cpp:246-248` — `read_bool` = `read_u8() != 0` (and C++
    `read_u8` is bitfield-masked; the bare `GameMemory` layer here stays unmasked, P4.2 composes).
  - `../primewatch2/src/defs/GameDefinitions.cpp:274-283` — `read_string`: `maxLen = 255`,
    null-terminated, byte-by-byte `read_u8`, raw bytes appended as `char` (ASCII, no byte-swap).
  - Current Rust: `src/mem/game_memory.rs:33-66` (the buggy `read_*` block); consumers-to-be:
    `src/structs/prime_structs.rs:223-225` (`GameInstance::read_u32`), P4.2.

  **Steps:**
  1. [x] Fix the bounded-slice bug in `read_u16` / `read_u32` / `read_u64` / `read_f32` / `read_f64`:
     replace `self.data[offset..].try_into().ok().map(T::from_be_bytes)` with
     `Some(T::from_be_bytes(self.data.get(offset..offset + N)?.try_into().ok()?))`. Add a private
     helper (fn or small macro) so the five bodies don't repeat the slice/try_into dance. Keep
     `from_be_bytes` as the single BE conversion point; keep routing through `address_to_offset`.
  2. [x] Add signed reads `read_i8` / `read_i16` / `read_i32` / `read_i64`, same pattern (`iN::from_be_bytes`,
     bounded slice, `Option<iN>`). `read_i8` mirrors `read_u8` (`self.data.get(offset).copied()` cast).
  3. [x] Add `read_bool(&self, address: u32) -> Option<bool>` → `Some(self.read_u8(address)? != 0)`
     (C++ `GameDefinitions.cpp:246`). No bitfield masking at this layer.
  4. [x] Add `read_string(&self, address: u32) -> Option<String>`: return `None` if the byte at
     `address` itself is OOB (`self.read_u8(address)` is `None`); otherwise loop `i in 0..255`,
     `read_u8(address.wrapping_add(i))`, break on `0` or on `None` (treat OOB as terminator, matching
     C++ `getRealPtr` returning 0), push `byte as char`. Return `Some(collected)`. Use an iterator or
     `#[allow(clippy::needless_range_loop)]` to keep clippy clean.
  5. [x] Add the bitfield primitive + masked integer reads. Private helper
     `fn extract_bits(v: u64, bit: u32, bit_length: u32, width_bits: u32) -> u64` implementing C++
     `getBits`: `mask = if bit_length == 0 || bit_length >= width_bits { u64::MAX } else { (1u64 << bit_length) - 1 }`;
     `shifted = if bit >= width_bits { 0 } else { v >> bit }`; `(shifted & mask)` then mask to
     `width_bits`. Then `read_u8_bits` / `read_u16_bits` / `read_u32_bits` / `read_u64_bits`
     `(address, bit: u32, bit_length: u32) -> Option<T>` that read the raw unsigned value, widen to
     `u64`, run `extract_bits`, narrow back with `as T`. `bit == 0 && bit_length == 0` must be
     identical to the plain `read_uN`. No `_bits` variant for floats (C++ `read_f32`/`read_f64` skip
     `getBits`).
  6. [x] Add `#[cfg(test)] mod tests`. Helper `fn load_mem1() -> Option<GameMemory>`: path from env
     `PRIMEWATCH_MEM1_RAW`, else `../primewatch2/mem1.raw`; if the file is absent return `None` and
     each test does `let Some(m) = load_mem1() else { return };` (skip-if-absent, no failure).
     Assertions against known-stable `mem1.raw` values (verified from the dump header):
       - `read_u8(0x8000_0000) == Some(0x47)` (`'G'`)
       - `read_string(0x8000_0000) == Some("GM8E01".to_string())`
       - `read_u32(0x8000_0000) == Some(0x474D_3845)` (`"GM8E"`)
       - `read_u16(0x8000_001C) == Some(0xC233)`
       - `read_u32(0x8000_001C) == Some(0xC233_9F3D)` (GameCube DVD magic word)
       - `read_u32(0x8000_0020) == Some(0x0D15_EA5E)` (disc magic)
       - `read_f32(0x8000_001C) == Some(f32::from_bits(0xC233_9F3D))`
       - masking: `read_u32(0x0000_001C) == read_u32(0x8000_001C)`
       - OOB: `read_u32(0x8000_0000 + 0x17F_FFFE) == None`; `read_u8(0x8000_0000 + 0x180_0000) == None`
     Bitfield assertions (pure logic — still fine to run against the file):
       - `read_u32_bits(0x8000_001C, 0, 0) == read_u32(0x8000_001C)`
       - `read_u32_bits(0x8000_001C, 4, 4) == Some(0x3)` (`(0xC2339F3D >> 4) & 0xF`)
       - `read_u32_bits(0x8000_001C, 0, 8) == Some(0x3D)`
     Plus a file-independent `extract_bits` unit test (mask width, `bit`/`bit_length` past width, the
     `length == 0` → full-mask case).
  7. [x] Gates: `cargo fmt` (2-space, no diff), `cargo build`, `cargo clippy --all-targets`,
     `cargo test` — all clean.

  **Watch for:**
  - **BE conversion stays in this layer only** (`from_be_bytes`); nothing above re-swaps. `read_string`
    bytes are raw ASCII — confirmed no byte-swap in C++ (`GameDefinitions.cpp:278` reads one `char` at
    a time). Do not `from_be_bytes` string bytes.
  - **`& 0x7FFFFFFF` masking**: keep every read going through `GameMemory::address_to_offset`; do not
    re-mask or index `self.data` directly with a raw address.
  - **Divergence from C++ `getRealPtr` (intentional, keep it):** C++ clamps an OOB masked address to
    offset `0` and reads garbage from the start of RAM. The Rust port returns `None` via `self.data.get(..)`.
    P4.2 `GameInstance` is responsible for choosing a default (0 / empty) when a read is `None` —
    note this in the impl notes so the reviewer doesn't "fix" it back to C++ behavior.
  - **Shift/mask UB:** C++ `getBits` uses `1ULL << length` (64-bit) so it is safe for `length < 64`;
    a naive Rust `(1u32 << 32)` or `v >> 32` panics in debug. Guard `bit >= width_bits` and
    `bit_length >= width_bits` explicitly in `extract_bits` (see step 5).
  - **`bit` / `bit_length` type:** `GameMember` stores them as `Option<i64>`
    (`src/structs/prime_structs.rs:168-169`). This task's helpers take plain `u32` (post-`value_or(0)`
    semantics); the `Option`→`u32` unwrap is P4.2's job at the `GameInstance` boundary — don't pull
    the defs' `i64`/`Option` types into `game_memory.rs`.
  - **No globals / explicit context:** all reads are `&self` on `GameMemory`; no `static`, no
    `OnceCell`. `Ctx<'a>` is P4.5 — don't introduce it here.
  - **Signature contract:** keep the existing `Option<T>` return convention (C++ returns `T` with an
    implicit 0-default; `Option` is the deliberate Rust improvement). P4.2's `GameInstance` typed
    reads depend on this being `Option`.
  - **Bug being fixed:** `self.data[offset..].try_into()` currently requires the *entire* remaining
    slice to be exactly `N` bytes, so `read_u16/u32/u64/f32/f64` return `None` for essentially every
    address (and panic when `offset > len` because the range start is out of bounds). After this task
    they must return the correct BE value for any in-range address.
  - 2-space rustfmt (`.rustfmt.toml`); `edition = "2024"`.
  - Scope guard: no per-frame refresh / Dolphin wiring (P3.2), no `GameInstance` changes (P4.2), no
    `.raw` load-path changes beyond what the test helper needs (`load_from_file` already exists).

  **Done when:**
  - `cargo build` and `cargo clippy --all-targets` are clean with no new warnings; `cargo fmt --check`
    is clean.
  - `cargo test` passes, and when `../primewatch2/mem1.raw` is present the new `game_memory` tests
    run (not skipped) and assert the `GM8E01` id at `0x0`, `0xC2339F3D` at `0x1C`, `0x0D15EA5E` at
    `0x20`, address masking equivalence, `None` on OOB, and the bitfield extraction cases.
  - `grep -n "OnceCell\|lazy_static\|static mut" src/mem/game_memory.rs` prints nothing.
  - `read_u16(0x8000001C)` on `mem1.raw` returns `Some(0xC233)` (the concrete misread-fix proof).
  - Committed with the `TASKS.md` promotion as `port(P3.1): round out GameMemory read surface`.

  **Implementation notes (P3.1):**
  - All fixed-width reads (`read_u16/u32/u64/i16/i32/i64/f32/f64`) now go through a private
    `fn read_bytes<const N: usize>(&self, address: u32) -> Option<[u8; N]>` helper: masks via
    `address_to_offset`, `self.data.get(offset..offset + N)?.try_into().ok()`. Each public method is a
    one-liner `Some(T::from_be_bytes(self.read_bytes(address)?))`. `from_be_bytes` stays the single BE
    conversion point. `read_u8`/`read_i8` still index a single byte directly (`.get(offset).copied()`).
  - `read_bool` = `Some(self.read_u8(address)? != 0)`. `read_string` returns `None` only if the first
    byte is OOB, else loops `0..255`, treating `Some(0) | None` as the terminator (later-OOB byte
    behaves like a NUL — matches C++ `getRealPtr` clamping to 0). Raw `byte as char`, no byte-swap.
  - `extract_bits(v, bit, bit_length, width_bits)` ports `getBits` with Rust shift-UB guards:
    `field_mask = u64::MAX` when `bit_length == 0 || bit_length >= width_bits` else `(1<<bit_length)-1`;
    `shifted = 0` when `bit >= width_bits`; result also `& value_mask` (`width_bits`). `read_uN_bits`
    widen to `u64`, run it, narrow with `as`. No float `_bits` variants.
  - **Divergence from C++ kept:** OOB reads return `None`, not a clamp-to-offset-0 garbage read.
    P4.2 `GameInstance` owns default substitution.
  - Bit-position semantics confirmed against `mem1.raw`: for BE value `0x1234`, `read_u16_bits(_, 0, 8)`
    == `0x34` (low bits), `read_u16_bits(_, 8, 8)` == `0x12`.
  - Tests: `blank()` helper builds `Box<GameMemory>` via `Box::new_zeroed().assume_init()` (one
    documented `unsafe`; `[u8; N]` all-zero is valid) — a plain `GameMemory::new()` puts a ~24 MiB
    array on the 2 MiB test-thread stack and aborts. `load_mem1()` reads the file with `std::fs::read`
    and `copy_from_slice`s into the boxed snapshot for the same reason (it does NOT call
    `load_from_file`). Skip-if-absent: `PRIMEWATCH_MEM1_RAW` env else `CARGO_MANIFEST_DIR/../primewatch2/mem1.raw`;
    `eprintln!` + early `return` when missing. All planner expected values verified against the real
    dump before hardcoding — every one matched (no corrections needed). 4 tests, all run (file present).
  - Gates: `cargo fmt --check` exit 0; `cargo build` clean; `cargo clippy --all-targets` — message-set
    diff vs. baseline is empty (no new warning kinds; test-target warning count actually dropped 23→21
    because the new methods are now exercised); `cargo test` 4 passed / 0 failed.

  **Review (P3.1):** `cargo fmt --check` exit 0; `cargo build` clean; `cargo clippy --all-targets`
  and `cargo test` (4 passed / 0 failed) clean — every warning is pre-existing dead-code in
  `src/mem/**` / `src/structs/**` / `bstruct/`; none newly introduced by `game_memory.rs`. Bounded
  `read_bytes<N>` fixes the unbounded-`self.data[offset..]` misread bug (which returned `None` for
  every non-terminal address and panicked when `offset > len`); `offset + N` cannot overflow `usize`
  (`offset` is masked to <= 0x7FFF_FFFF, `N` <= 8) and `.get(range)` yields `None` out of range.
  `extract_bits` matches C++ `getBits` arm-by-arm: `bit_length == 0` -> full `value_mask` (== C++
  `~0` truncated to `T`); `1 <= bit_length < width` -> `(1<<bit_length)-1`; `bit_length >= width` ->
  full mask (== C++ `(1ULL<<length)-1` truncated to `T`); `bit >= width` -> 0 (guards Rust shift UB;
  C++ would be UB here too, schema never emits it). Final `& value_mask` reproduces the C++ `T`
  narrowing. `read_string` matches C++ `GameMember::read_string` for ASCII (255 cap, NUL terminator,
  raw byte -> `char`); documented deviations: returns `None` if the first byte is OOB, treats a
  later OOB byte as a terminator, and non-ASCII bytes become multi-byte UTF-8 rather than raw bytes
  — all task-sanctioned and irrelevant to the struct-name use case. BE conversion is `from_be_bytes`
  only in this layer; every read routes through `address_to_offset` (`& 0x7FFFFFFF`); no globals
  (`grep` clean). OOB-returns-`None` divergence from C++ `getRealPtr` clamp-to-0 is intentional and
  owned by P4.2. Independently re-verified against `mem1.raw`: bytes `0x0..0x8` = `GM8E01\x00\x00`
  (so `read_u8(0)=0x47`, `read_string(0)="GM8E01"`, `read_u32(0)=0x474D3845`); bytes `0x1C..0x20` =
  `c2 33 9f 3d` (`read_u16=0xC233`, `read_u32=0xC2339F3D`); bytes `0x20..0x24` = `0d 15 ea 5e`
  (`read_u32(0x20)=0x0D15EA5E`); file length `0x1800000` confirms the OOB cases. `reads_against_mem1`
  ran (not skipped) — the dump is present at `../primewatch2/mem1.raw`. `blank()` unsafe
  (`Box::new_zeroed().assume_init()`) is sound: `GameMemory` is a single `[u8; N]` field, all-zero
  is valid, no niche/`NonZero`/enum/ref members. The latent ~24 MiB stack-overflow in
  `GameMemory::new()` / `load_from_file` predates this task and is out of P3.1 scope (read surface
  only); it is recorded as a standalone TODO under P3.2 — accepted as a deferral.
