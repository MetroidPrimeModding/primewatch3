# P9.1 — archived completed task

- Task id: **P9.1**
- Status: `DONE` (archived completed task)
- Commit: the commit that adds this file (`port(P9.1): …`).
- Summary and forward-relevant facts live in [`TASKS.md`](../TASKS.md); this file is the full historical record.

**Review (port-reviewer):** Verified `src/app.rs` / `src/world/renderer.rs` against `PrimeWatch.cpp`
(`mainLoop` / `doFrame` / `doImGui` / `doMainMenu` / `doMemoryParse`) and `processInput`
(`PrimeWatchInput.cpp:126-233`). `InputState::plan` reproduces the C++ branch-for-branch: sticky
capture (133-140), Shift/Ctrl+1-5 ghost record/clear (146-158, not keyboard-gated in either), mouse
look `*0.005`/`-0.005` gated on `capturedMouse` (168-175), wheel zoom `*-2` gated on
`!WantCaptureMouse` (177-180), arrows `±0.03` / PageUp-Down `∓0.5` additive under
`!WantCaptureKeyboard` (182-204), and the `Detached`-only WASD/QE net-axis move (206-231).
`record_player_ghost` / `clear_player_ghost` / `move_detached_camera` on `WorldRenderer` match the
`worldRenderer.*` mutations `processInput` performs (world-Z up axis un-rotated, `manualCameraSpeed
* 0.2`, `quat(vec3(0,0,yaw))` basis). Frame order in `App::redraw` → `AppWindow::render` is
input → `update_from_dolphin` → `get_all_objects` → `world.update` (CPU) → `world.render` (GPU) →
egui pass → composite, all defs-gated; C++ builds the egui UI slightly earlier (before
`worldRenderer.update`), a sub-frame data-freshness difference on the status readouts only, not a
correctness defect. Menu structure (Attach / Culling-Camera-Triggers-Actors / Tools) and order
match `doMainMenu`; globals window, raw view, camera-controls window match `doImGui`. No new
globals / `static mut`; `Ctx` threaded; `rfd` was the task-named picker crate. `cargo fmt --check`
clean; `cargo test` 112 passed; `cargo clippy --all-targets` clean (only pre-existing dead-code in
other files + bstruct submodule; none in `app.rs` / `renderer.rs`).

**Non-blocking follow-ups (carry into P9.2 / a shell-polish pass):**
1. Camera Controls window has no `.open()` X-close binding (P8.4.6 deferred this to P9.1); it is
   still dismissable via the Camera menu "Show camera controls" toggle, so only the C++ titlebar-X
   path is missing.
2. Scroll-to-zoom is dead while the pointer merely hovers the "World" egui window (pattern-B
   consequence — `egui_wants_pointer_input()` is global). Consider gating world scroll/look on the
   World `Image` response's `hovered()`/`dragged()` instead of the global flag.
3. `rfd 0.15` pulls a heavy `zbus`/`ashpd`/async tree (xdg-portal backend) + a second `pollster`;
   `LoadFromFile` also blocks the event loop synchronously. Revisit feature-trimming / the async
   API before P10 packaging.
4. Shell layout is rough — the `menu_bar` and `world-status-host` Areas are both near the
   top-left (`world-status-host` nudged to `y=24`).

**Manual verification (human, 2026-09-01):**
- [x] Arrow keys in the 3D view move camera pitch/yaw; in Detached mode WASD/QE move the camera.
- [x] Menu items respond to clicks; Attach lists Dolphin PIDs and attaches/detaches.
- [x] "Load from file" opens a native picker and loads a dump (detaching from Dolphin first).
- [x] Text overlays render at the right screen positions over the World image.
- [ ] Shift+1-5 record player ghosts and Ctrl+1-5 clear them — still untested.
- [ ] "Reload Definitions" / the NOT-LOADED "Reload" button reload `prime_defs/` and update the status
  text — still untested.
- [ ] Live-Dolphin attach path (vs. the verified `.raw` load path) — still untested.
- [ ] Mouse capture grabs/hides the cursor only on the button-down→capture transition, not every frame
  — not separately confirmed.

---

- [ ] **P9.1** winit event loop: input → per-frame memory parse → egui UI → 3D render, in that order. — `IN REVIEW`

  **Steps:**
  1. [x] Wire winit `WindowEvent::KeyboardInput` + mouse delta + scroll wheel into `WorldInput` (cam_pitch, cam_yaw, cam_zoom).
     - Mouse capture state: ungrab when egui wants mouse; grab when left/right button pressed and egui doesn't want it.
     - Movement keys (WASD/QEDF for detached camera, arrow keys for all modes, PageUp/PageDown for zoom).
     - Shift+1-5 to record player ghosts; Ctrl+1-5 to clear them.
     - Route through `egui_state.on_window_event` first so egui can claim input.
  2. [x] Flesh out the Attach and Tools menus (marked `// P9.1:` in `app.rs::render`).
     - Attach menu: list available Dolphin PIDs (refresh on-demand), select to attach, detach button, "Load from file" dialog.
     - Tools menu: Reload Definitions, Raw Data View checkbox, Demo View checkbox, exact-values toggle.
  3. [x] Wire file dialogs for "Load from file" (use `rfd` crate for native file picker).
  4. [x] Implement Raw Data View window (egui-based hex dump or use `egui_memory_editor` if it's compatible).
  5. [x] Paint the queued text overlays: iterate `worldRenderer.text_overlays` and render each as screen-space egui text (Y-flipped coordinates, centered).
  6. [x] Verify frame ordering: input processed → memory updated → objects walked → UI built → render executed. Should be a no-op fix if step 1 routes input correctly.

  **Implementation notes (port-implementer):**
  - **What shipped** (`src/app.rs`, near-total rewrite; `src/world/renderer.rs` +3 methods; `Cargo.toml` +`rfd = "0.15"`):
    - `InputState` (accumulates `keys_down` / `modifiers` / raw `mouse_delta` / `scroll` / button state / sticky `captured_mouse`) fed by `App::accumulate_input` from `WindowEvent`s + `App::device_event` (`DeviceEvent::MouseMotion` for the look delta). `InputState::plan(wants_kb, wants_mouse, camera_mode) -> InputPlan` is a **pure** port of `PrimeWatch::processInput` (`PrimeWatchInput.cpp:126-233`): sticky capture, mouse look (`*0.005`/`-0.005`), wheel zoom (`*-2`), arrows (`±0.03`), PageUp/Down (`∓0.5`), and the `Detached`-only WASD/QE net-axis vector. 6 unit tests.
    - `App::redraw` = C++ `mainLoop` body: `update_from_dolphin` → apply input plan (`WorldRenderer::record_player_ghost` / `clear_player_ghost` / `move_detached_camera`, cursor grab on capture transition) → `get_all_objects` → `world.update` → `AppWindow::render`.
    - `AppWindow::render` now takes `&mut FrameState<'_>` (mutable game/UI state borrowed out of `App` via a destructure of `&mut self` — avoids an `App`↔`AppWindow` cycle). Attach menu (Refresh / PID list / Detach / Load from file) and Tools menu (Reload Definitions / Raw Data View / Raw Demo View / exact-values) inlined into the existing `MenuBar` closure. Menu clicks push a `MenuAction` collected during the (shared-borrow-only) egui pass and applied by `apply_menu_action` after `queue.present`.
    - `render_raw_data_view`: custom monospace hex table via `ScrollArea::show_rows` over `mem.data` (no `egui_memory_editor` — not needed). Offsets are base-0 snapshot offsets, matching the C++ `MemoryEditor`.
    - Text overlays: painted in the "World" window closure, `ov.screen_pos` (world-target physical px, Y-down) scaled into the `ui.image` rect and drawn with `painter.text(.., Align2::CENTER_CENTER, ..)`.
    - "globals" window (C++ `doImGui:314-320`) mounted via the P7 `Inspector` (`g_stateManager` / `g_main` / `gp_MemoryCard` / `gp_TweakPlayer`). "Reload" button added to the NOT-LOADED window (the P1.2 follow-up).
  - **Deviations / watch-closely:**
    - Mouse look uses raw `DeviceEvent::MouseMotion` deltas, not screen-space `io.MouseDelta`; the `0.005` factor is unchanged but feel may differ.
    - The 3D view lives inside an egui `Window` (P1.3 pattern B), so `egui_wants_pointer_input()` is `true` while merely *hovering* it → **scroll-to-zoom does nothing while the pointer is over the World window**. Camera drag still works: `wants_pointer_input()` flips to `false` once a button goes down over the (non-interactive) image, so click-drag capture engages as in C++.
    - `MenuAction::LoadFromFile` calls `rfd::FileDialog::pick_file()` synchronously — blocks the event loop while the native dialog is open (C++ `ImGuiFileDialog` polled per frame). `rfd 0.15` pulls `ashpd`/`zbus` transitively (xdg-portal backend) + a second `pollster 0.4`; compiles without a display, runtime-only dependency on a portal.
    - "Raw Demo View" is a placeholder window (label only) — `egui_demo_lib` is not a dependency.
    - Menu-bar `Area` and `world-status-host` `Area` still both near the top-left; status host nudged to `y=24`. Full shell layout is still rough — window placement is cascade-default.
    - `ReloadDefs` rebuilds `GameStructs` from scratch (`new_empty()` then `load_from_dir`) so removed `.bs` entries don't linger.
  - **Not run here (no display):** all "Done when" manual items (Shift+arrows move the camera, menu clicks, file picker, reload) stay **pending for the human**. Compile + headless tests only.
  - **Forward-deps for P9.2:** `FrameState` is the seam — add the object-table/filter + per-editor-ID watch windows in `AppWindow::render` next to "globals"; `App.objects` is walked every frame and needs to be threaded into `FrameState` (currently dropped after `world.update`). Real `highlighted` set (currently empty `// TODO(P9.2)` in `redraw`) feeds `world.update` + the table's hover row.

  **Port from:**
  - `../primewatch2/src/PrimeWatchInput.cpp:126-233:processInput` — mouse/keyboard input capture, camera delta/movement accumulation for all modes (follow-player, game-cam, detached), player ghost save/restore via Shift/Ctrl+1-5. Input is reset at frame start, accumulated from egui IO state, and routed to camera update.
  - `../primewatch2/src/PrimeWatch.cpp:350-481:doMainMenu` — menu bar structure: Attach (with submenu for Refresh/list PIDs/detach/load dialog), Culling/Camera/Triggers/Actors (already ported P8.4.6), Tools (Reload Definitions, Raw Data View, Demo Window, exact-values toggle). Culling/Camera/Triggers/Actors already exist in `worldRenderer.render_menu`; Attach/Tools are the stubs.
  - `../primewatch2/src/PrimeWatch.cpp:280-320:doImGui` — file dialog host (ImGuiFileDialog integration for load-from-file path), raw data view (mem_edit embedded viewer), globals inspector windows, camera controls window (already ported). The file dialog responds to menu clicks and loads a binary dump on success, detaching from Dolphin first.

  **Watch for:**
  - Egui 0.36 has no context-level `TopBottomPanel` — use `Area + Frame::menu + MenuBar` pattern (already done in current `app.rs`).
  - Mouse capture: don't grab/ungrab on every frame; the `egui_state.on_window_event` call already updates the IO state. Check `io.want_mouse` before processing motion/scroll.
  - Ghost array mutation: `worldRenderer.player_ghosts[i]` needs mutable access; don't panic on out-of-bounds.
  - File dialog: `rfd::FileDialog` on Linux/macOS/Windows (cross-platform); the picker should start in the current directory and default to `.raw` files. The C++ uses `ImGuiFileDialog`, which handles cancellation natively; `rfd` returns `None` on cancel.
  - Overlay text: `WorldRenderer::project` returns NDC, not screen pixels — convert to egui's top-left origin space (Y-flip at `viewport.height - screen_pos.y`), then call `ui.painter().text()` or similar. Check that centered text doesn't collide with the World panel's border.
  - The "Reload Definitions" button in Tools must call `self.structs.load_from_dir("prime_defs")` and refresh `self.defs_loaded` / `self.status_text` — requires `&mut self` capture in the menu closure (egui 0.36 closure takes `&mut Ui`).

  **Done when:**
  - `cargo build` clean.
  - `cargo clippy --all-targets` clean (with the usual dead-code warnings for unused overlay fields).
  - Manually: running the app, pressing Shift+arrow keys in the 3D view moves the detached camera; menu items respond to clicks; "Load from file" opens a file picker and loads a `.raw` dump; "Reload Definitions" reloads `prime_defs/` and updates the status. (Requires a display and preferably a live Dolphin or `mem1.raw` in the working directory.)
