# P5.2 — archived completed task

Archived completed task. Carried by the commit that adds this file (`port(P5.2): …` on
`rust-conversion`). Summary lives in [`TASKS.md`](../TASKS.md); this file is the full entry.

---

- [x] **P5.2** Port the vtable `address → class name` map. — `DONE`

  **Port from:** `../primewatch2/src/defs/GameVtables.cpp:3-146` (the whole `MP1_VTABLES`
  initializer, 142 pairs) + `../primewatch2/src/defs/GameVtables.hpp` (just `extern std::map<...>`).
  Consumers for the behavioral spec: `../primewatch2/src/utils/GameObjectUtils.cpp:46-51`
  (`if (MP1_VTABLES.count(vtable)) { type = MP1_VTABLES[vtable]; ... }`) and
  `../primewatch2/src/PrimeWatch.cpp:527,568` (membership test + lookup for the "unknown vtables"
  UI). Every use is a point lookup.

  **Steps:**
  - [x] 1. New module `src/mem/vtables.rs`; `pub mod vtables;` in `src/mem/mod.rs`.
  - [x] 2. `pub static MP1_VTABLES: LazyLock<HashMap<u32, &'static str>>` — all 142 entries
     transcribed verbatim, including the unresolved-alias value `"0x803d9e30"` and the
     "also possibly …" / "TODO: check this one" comments.
  - [x] 3. `pub fn vtable_class_name(address: u32) -> Option<&'static str>` — the
     `.count()?.[] ` convenience.
  - [x] 4. Tests: known entries resolve (incl. the alias string), unknowns → `None`, and a
     `MP1_VTABLES.len() == 142` guard against a dropped/duplicated transcription line.

  **Watch for:**
  - Edition 2024 → `std::sync::LazyLock` is available; no `once_cell` / `lazy_static` / `phf` dep.
  - A duplicate key in the source would silently shrink the `HashMap` — the `len()` test catches it.
    Verified against the C++ with `grep -oE '\{0x[0-9a-f]+,' | sort | uniq -d` (no dupes; 142 keys).
  - `.rustfmt.toml` 2-space indent; `cargo clippy --all-targets` + `cargo test` clean.

  **Done when:** the map + lookup exist in `src/mem/vtables.rs`, wired into `mem/mod.rs`, tests
  green.

  **Implementation notes (P5.2):**
  - `src/mem/vtables.rs`: module doc explains the `+0x0` vtable-pointer identity trick and that
    P6.1's `CObjectList` walk is the real consumer. `MP1_VTABLES` is a `LazyLock<HashMap>`;
    `vtable_class_name` is the point-lookup helper. Both `pub` — later phases (P9 "unknown vtables"
    UI) can iterate `MP1_VTABLES` directly.
  - 142 entries, transcription verified by `grep -cE '^\s*\(0x'` (142) and `uniq -d` (no dup keys)
    on both the C++ and Rust sides.
  - 3 tests. `cargo test`: 24 passed / 0 failed. `cargo clippy --all-targets`: +2 dead-code
    warnings (`MP1_VTABLES` / `vtable_class_name` never used) — consistent with the not-yet-wired
    defs layer, cleared when P6.1 calls them. `cargo fmt --check` clean.
  - **P6.1 forward-note:** the retype guard in C++ is `MP1_VTABLES.count(v) && structByName(name)` —
    the walk must still confirm the mapped name is a known `.bs` struct before retyping the handle.

  **Review:** Diffed all 142 `(addr, name)` pairs against `GameVtables.cpp` — exact match including
  the `0x803d9ce0 → "0x803d9e30"` alias. `vtable_class_name` reproduces the C++
  `count() ? map[] : none` semantics. `LazyLock` (edition 2024, no new dep) is the right call.
  `len() == 142` guard + `uniq -d` check rule out a silent dup-key collapse. `cargo test` 24/24;
  clippy carries only the expected not-yet-wired dead-code warnings. Completes Phase 5.
