# P4.3 — archived completed task

Array-element indexing on `GameMember`/`GameInstance`. Status: `DONE`.
Carried by the commit that adds this file (`port(P4.3): ...`). See `TASKS.md` for the forward-facing summary.

---

- [x] **P4.3** Array-element indexing on `GameMember`/`GameInstance`. — `IN REVIEW`

  **Steps:**
  1. [x] `src/structs/prime_structs.rs` — add a free `pub fn primitive_size(type_name: &str) -> u32`
     porting C++ `GameDefinitions::primitiveSize` **exactly**, including the `_ => 4` default:
     `u8|i8|bool => 1`, `u16|i16 => 2`, `u32|i32|f32 => 4`, `f64 => 8`, everything else `=> 4`.
     (C++ has no `u64`/`i64` case — it falls through to 4. Match that; do not "fix" it.)
  2. [x] Add `array_length: Option<i64>` to `GameInstance` (next to `bit`/`bit_length`). `None` from
     `new` and `with_bitfield` (keep those signatures — `globals.rs` roots stay put); populate it in
     `get_member` from `member.array_length`. Fold the growing field set into a single private
     `with_member(addr, member)` helper if it reads cleaner, but don't change the public ctors.
  3. [x] Add `GameInstance::element_size(&self, structs: &GameStructs) -> u32` — C++ `renderArray`
     lines 433-439 / `renderVector` 406-411: `structs.get_struct_by_name(&self.type_name).map(|s|
     s.size as u32).unwrap_or_else(|| primitive_size(&self.type_name))`. Guard `s.size` negative →
     `.max(0) as u32`.
  4. [x] Add `GameInstance::element(&self, structs: &GameStructs, index: u32) -> GameInstance` — ports
     the C++ `arrayItem.offset = member.offset + i * sizePer` step (`GameObjectRenderers.cpp:442-448`):
     new instance at `self.address + index * self.element_size(structs)`, same `type_name`,
     `array_length: None`, `bit`/`bit_length` cleared (`None`) — an array element is never itself a
     bitfield (C++ `arrayItem.arrayLength = nullopt`; bit/bitLength were already unset on an array
     member). **Not** `Option` — pure address arithmetic, mirrors the C++ which never bounds-checks
     here. Callers bound-check against `array_length` themselves (P6/P7).
  5. [x] Tests in `prime_structs.rs`:
     - `primitive_size` table incl. the `u64`→4 fallthrough and an unknown-type→4.
     - In-memory schema: a struct with an `u32[8]` member and a `Foo[4]` member (`Foo` = 12-byte
       struct); `get_member(...)` carries `array_length`; `.element_size()` = 4 and 12 respectively;
       `.element(structs, 3).address` = base + 12 and base + 36; element has `array_length == None`.
     - `mem1.raw` (skip-if-absent): index a real primitive array via `get_member().element(_, n)`
       and assert `read_u32` matches a hand-computed `GameMemory::read_u32(base + n*4)`.

  **Port from:**
  - `../primewatch2/src/defs/GameDefinitions.cpp:75-96:primitiveSize` — the size table + default 4.
  - `../primewatch2/src/defs/GameObjectRenderers.cpp:425-451:renderArray` — `sizePer` =
    `structByName(type).size` else `primitiveSize(type)`; element `i` at
    `member.offset + i * sizePer`; element clears `arrayLength`.
  - `../primewatch2/src/defs/GameObjectRenderers.cpp:403-419:renderVector` (context only) —
    same `sizePer` rule applied to a `first` member + `sizePer * index`. `rstl::vector`/`rstl::rc_ptr`
    element traversal (needs a runtime count / `first` pointer) is **P6/P7 scope**, not here — P4.3
    is just the fixed-stride index step.

  **Watch for:**
  - `.rustfmt.toml` = 2-space indent. `cargo clippy --all-targets` + `cargo test` clean.
  - `get_member` currently does `self.address + member.offset as u32`; keep the element math the
    same style (`u32` wrapping is fine — C++ uses `uint32_t`).
  - Don't add a `Ctx` (P4.5). Keep explicit `&GameStructs`.
  - Don't bounds-check `index` against `array_length` in `element` — C++ doesn't, and P4.4/P6/P7
    own the panic/clamp policy.

  **Done when:** `primitive_size`, `GameInstance::{element_size, element}` and the carried
  `array_length` field exist and match the C++ stride math; tests prove the size table, struct vs
  primitive stride, address arithmetic, field-clearing, and a `mem1.raw` round-trip; clippy + test
  green.

  **Implementation notes:**
  - `src/structs/prime_structs.rs` only. Added free `primitive_size` (exact port of
    `GameDefinitions::primitiveSize`, `u64`/`i64` deliberately fall through to `4`).
  - `GameInstance` gained `array_length: Option<i64>`; `new` / `with_bitfield` signatures
    unchanged (both set it `None`). Added private `with_member(addr, &GameMember)` helper that
    carries `bit` / `bit_length` / `array_length`; `get_member` now uses it (pointer auto-deref
    path unchanged) so it also carries `array_length`.
  - `element_size` clamps a negative struct `size` with `.max(0) as u32`. `element` uses
    `wrapping_add` / `wrapping_mul` (C++ `uint32_t` semantics), returns a plain `GameInstance`
    via `new` so `bit` / `bit_length` / `array_length` are all `None`; no bounds check on
    `index` (matches C++).
  - Tests added: `primitive_size_table` (incl. `u64`/`i64`→4 and unknown→4),
    `element_indexing_stride_and_field_clearing` (in-memory `u32[8]` + `Foo[4]` with `Foo` a
    12-byte struct; stride 4 / 12, addresses base+0x10+12 and base+0x40+36, field-clearing),
    `element_reads_match_raw_memory` (mem1.raw skip-if-absent, round-trips 6 `u32` elements vs
    `GameMemory::read_u32(base + n*4)`). Added `game_struct_sized` test helper.
  - `cargo fmt` clean. `cargo clippy --all-targets`: no errors; the pre-existing mid-port
    dead-code / collapsible-if warning pile is unchanged except one new `function
    'primitive_size' is never used` (same class as the existing unused `GameInstance` surface —
    wired up in P6/P7). `cargo test`: 15 passed / 0 failed (mem1.raw present in this env).

  **Review:** `primitive_size` table verified char-for-char against `GameDefinitions.cpp:75-96`
  (u64/i64 correctly absent → `_ => 4`). `element_size` / `element` match the `renderArray`
  `sizePer` + `member.offset + i*sizePer` stride (`GameObjectRenderers.cpp:432-448`); `self.address`
  already includes `member.offset` so the arithmetic lines up. Element clears bit/bit_length/
  array_length (minor sanctioned deviation from the literal C++ `arrayItem = member` copy, which
  would carry bit/bitLength — irrelevant for array members). No bounds check, `wrapping_*` math,
  ctors unchanged (globals.rs untouched). `cargo fmt --check` clean; `cargo test` 15 passed.
  Dead-code warning on `primitive_size` left unsuppressed — consistent with the rest of the
  not-yet-wired defs layer, acceptable.
