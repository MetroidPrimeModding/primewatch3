# P10.1 — archived completed task

- Task: **P10.1** Rust CI (`cargo build --release`), zip binary + current `prime_defs/` — expanded to a cross-platform matrix that subsumes P10.2
- Status: `DONE`
- Commit: the commit that adds this file (`port(P10.1): ...` on `rust-conversion`).
- Summary lives in [`TASKS.md`](../TASKS.md); this file is the full verbatim entry as reviewed.

---

- [x] **P10.1** Rust CI (`cargo build --release`), zip binary + current `prime_defs/`. — `DONE`

  Port `../primewatch2/.github/workflows/cmake_build.yml` to a Rust build for this repo
  (`MetroidPrimeModding/primewatch3`). This repo currently has **no `.github/`**.

  **Port from:**
  - `../primewatch2/.github/workflows/cmake_build.yml:1-52` — the whole file. Behavioral spec:
    trigger on push to `main` and on `v[0-9]+.*` tags; `windows-latest`; `contents: write`
    permission; checkout with submodules; stable Rust toolchain; build Release; assemble a
    `primewatch3/` dir containing `prime_defs/` + the binary; `tar -a -c -f primewatch3.zip
    primewatch3`; upload the dir as an artifact (`if-no-files-found: error`); on a `v*` tag,
    `softprops/action-gh-release@v2` with `primewatch3.zip`.
  - `../primewatch2/RUST_CONVERSION.md:116-118` — P10 intent: `cargo build --release`, zip the
    now-current `prime_defs/` alongside the binary.

  **Scope note (user decision, post first implementation pass):** P10.1 is now **cross-platform**
  and **subsumes P10.2** — build/test/package on a `windows-latest` / `ubuntu-latest` /
  `macos-latest` matrix in one workflow. `Pwootage/bstruct_rust` is confirmed **public** (submodule
  checkout works with the default `GITHUB_TOKEN`; the caveat below is informational only).

  **Steps:**
  1. [x] `.github/workflows/rust_build.yml`: name `Rust`; `on: push: branches:[main], tags:['v[0-9]+.*']`.
  2. [x] Job `build` on `${{ matrix.os }}` = { windows-latest, ubuntu-latest, macos-latest },
     `strategy.fail-fast: false`, `permissions: contents: write`. Per-leg `matrix.include` carries
     `bin` (`primewatch3.exe` vs `primewatch3`) and `archive`
     (`primewatch3-windows.zip` / `primewatch3-linux.tar.gz` / `primewatch3-macos.zip`).
  3. [x] `actions/checkout@v4` with `submodules: recursive`.
  4. [x] Linux-only `apt-get` step (`if: runner.os == 'Linux'`) installing the compile-time system
     deps for the winit/wgpu/rfd stack: `libgtk-3-dev libudev-dev libwayland-dev libxkbcommon-dev
     libx11-dev libxrandr-dev libxi-dev libxcursor-dev`.
  5. [x] `dtolnay/rust-toolchain@stable`; `Swatinem/rust-cache@v2` with `key: ${{ matrix.os }}`.
  6. [x] `cargo build --release` then `cargo test --release` (all three legs; tests skip-if-absent
     without `mem1.raw`).
  7. [x] Package step, `shell: bash` on all OSes: `mkdir primewatch3`; `cp -r prime_defs primewatch3/`;
     `cp target/release/${{ matrix.bin }} primewatch3/`; `tar -a -c -f ${{ matrix.archive }} primewatch3`.
  8. [x] `actions/upload-artifact@v4` name `primewatch3-${{ matrix.os }}` (unique per leg), path
     `primewatch3/**`, `if-no-files-found: error`.
  9. [x] `softprops/action-gh-release@v2` in every matrix leg, guarded by
     `startsWith(github.ref, 'refs/tags/v')`, `files: ${{ matrix.archive }}`,
     `fail_on_unmatched_files: true`, `body: Automated release build` — the three legs upsert the
     same release and each attaches its own archive.

  **Watch for:**
  - Binary name is `primewatch3.exe` on Windows, `primewatch3` on Linux/macOS (crate `name = "primewatch3"`).
  - `tar -a -c -f` is uniform across legs: bsdtar on windows/macos infers `zip` from `.zip`;
    GNU tar on ubuntu infers `gzip` from `.tar.gz` (`-a` = `--auto-compress`). GNU tar can't write
    zip, hence Linux uses `.tar.gz`.
  - `.cargo/cargo.toml` is misnamed and inert (CLAUDE.md) — no linker override applies on CI; the
    default per-OS linkers are fine. Do not rename it as part of this task.
  - Submodule `bstruct` URL is `git@github.com:Pwootage/bstruct_rust.git` — account `Pwootage`, not
    this repo's org `MetroidPrimeModding`. `actions/checkout` rewrites SSH→HTTPS + injects
    `GITHUB_TOKEN`. Confirmed **public** by the user, so this works; if it ever goes private, CI
    needs a PAT secret wired into `actions/checkout`.
  - `edition = "2024"` needs Rust ≥ 1.85; `@stable` on GitHub runners satisfies this (2026).
  - YAML can't be executed here and no runner is available — verification is `cargo build --release`
    on Linux (proxy) + `yaml.safe_load` parse. Live CI run (all three OSes + a `v*` tag) is a
    user/manual check.

  **Done when:** `.github/workflows/rust_build.yml` exists, is valid YAML, mirrors the C++
  workflow's triggers/packaging/release behavior for `primewatch3`, and `cargo build --release`
  is confirmed green locally. Manual check (push to a branch / tag, watch Actions) is deferred to
  the user.

  **Implementation notes:**
  - New file only: `/mnt/host/primewatch3/.github/workflows/rust_build.yml` (repo had no
    `.github/`). No Rust source changed; `cargo clippy`/`cargo fmt` deliberately not re-run
    (nothing to format).
  - Ported `../primewatch2/.github/workflows/cmake_build.yml`, swapping the CMake configure/build
    for `cargo build --release` + `cargo test --release`. Job name `Rust`, `permissions: contents:
    write`, triggers = push to `main` + `v[0-9]+.*` tags — all matching the C++ workflow.
  - **Cross-platform (folds in P10.2 per user decision):** `strategy.matrix.include` over
    `windows-latest` / `ubuntu-latest` / `macos-latest`, `fail-fast: false`. Each leg carries
    `bin` + `archive` matrix vars. Archives: `primewatch3-windows.zip`,
    `primewatch3-linux.tar.gz`, `primewatch3-macos.zip`; each contains the same `primewatch3/` dir
    layout (`prime_defs/` + binary) as the original C++ zip.
  - `tar -a -c -f "${{ matrix.archive }}"` is uniform across all legs under `shell: bash`: bsdtar
    (win/mac) picks `zip` from `.zip`, GNU tar (linux) picks `gzip` from `.tar.gz` via
    `--auto-compress`. Linux is `.tar.gz` because GNU tar cannot write zip.
  - Linux-only `apt-get` step (`if: runner.os == 'Linux'`, before toolchain) installs
    `libgtk-3-dev libudev-dev libwayland-dev libxkbcommon-dev libx11-dev libxrandr-dev libxi-dev
    libxcursor-dev` — compile-time deps for winit/wgpu/rfd(ashpd/zbus). Mesa/Vulkan not installed
    (not needed to *build*).
  - `Swatinem/rust-cache@v2` gets an explicit `key: ${{ matrix.os }}` so the three OS caches
    don't collide.
  - `softprops/action-gh-release@v2` runs in every matrix leg (guarded by the `refs/tags/v`
    prefix), `files: ${{ matrix.archive }}`, `fail_on_unmatched_files: true`. The action upserts a
    single release per tag and merges `files:` across legs, so the release ends up with all three
    archives.
  - `actions/upload-artifact@v4` name is `primewatch3-${{ matrix.os }}` (v4 requires unique
    artifact names per run); path `primewatch3/**`, `if-no-files-found: error`.
  - Other deviations vs the C++ file: `submodules: recursive` (was `true`; equivalent — bstruct has
    no nested submodules); dropped `env: BUILD_TYPE` + the redundant `with: toolchain: stable`;
    added `env: CARGO_TERM_COLOR: always`.
  - CAVEAT (informational — user confirmed `Pwootage/bstruct_rust` is **public**): the submodule
    URL `git@github.com:Pwootage/bstruct_rust.git` is a different account than the repo org
    `MetroidPrimeModding`. `actions/checkout` rewrites SSH→HTTPS + injects `GITHUB_TOKEN`; this
    works while the repo is public. If it ever goes private, `actions/checkout` needs a PAT secret
    (`token:` / `ssh-key:`) — not added here.
  - PENDING MANUAL (user) CHECK: no CI runner and no Windows/macOS box in this environment.
    Verified here by (a) `cargo build --release` green on Linux (proxy — produced
    `target/release/primewatch3`; `cargo test --release` = 119 passed), (b) YAML parses with
    `python3 -c "import yaml; yaml.safe_load(open(...))"`. A live CI run across all three OSes
    (push a branch, then a `v*` tag) is the user's to confirm — especially the Linux `apt` list
    (whether wgpu/rfd need anything more) and bsdtar zip creation on the runners.

  **Review (P10.1 — reviewer):**
  - YAML parses (`yaml.safe_load`). `cargo build --release` + `cargo test --release` green on this
    Linux box (119 passed, 0 failed); `cargo fmt --check` clean (no Rust changed).
  - Workflow faithfully mirrors `cmake_build.yml`: same triggers (`push` to `main` + `v[0-9]+.*`
    tags), job-level `permissions: contents: write`, `actions/checkout@v4` with submodules, stable
    toolchain, Release build, `primewatch3/` dir = `prime_defs/` + per-OS binary, `upload-artifact@v4`
    with `if-no-files-found: error`, `softprops/action-gh-release@v2` gated on `refs/tags/v`.
    Cross-platform matrix additions (windows/ubuntu/macos, `fail-fast: false`, per-leg `bin`/`archive`,
    unique artifact names, Linux apt step) all match the task Steps.
  - Binary paths correct (`target/release/primewatch3[.exe]`). Artifact names
    `primewatch3-{windows,ubuntu,macos}-latest` are unique (v4 requirement met). Release step in
    every leg with `files: ${{ matrix.archive }}` + `fail_on_unmatched_files: true` — the action
    upserts one release per tag and merges files, so the tag release ends with all three archives.
  - apt list checked against `Cargo.lock`: rfd resolves to the `ashpd`/`zbus` (xdg-portal) backend,
    not `gtk-sys`; `x11-dl`/`xkbcommon-dl`/`wayland-sys` are dlopen. So none of the apt `-dev`
    packages are strictly build-time required (this Linux box builds clean), but they are the correct
    runtime deps and a harmless superset — not a defect.
  - Linux `.tar.gz` choice is sound: GNU tar cannot create zip, and `-a` on an unrecognized `.zip`
    suffix would silently produce a non-zip; `.tar.gz` avoids that entirely.
  - NOT BLOCKING per the reviewer brief (multi-OS CI is an explicit pending user check), but flagged
    for the first CI run: the `tar -a -c -f *.zip` step on the **windows-latest** leg runs under
    `shell: bash` (Git-for-Windows bash). Git for Windows ships **GNU tar**, not bsdtar; if `/usr/bin/tar`
    shadows `C:\Windows\System32\tar.exe` (bsdtar) on the runner, the Windows leg would emit a
    mislabeled non-zip named `primewatch3-windows.zip` without failing the job. The Watch-for note
    assumes bsdtar on Windows — verify on the first run; if wrong, switch the Windows leg to
    `7z a` / `Compress-Archive` or force System32 `tar`. macOS `tar` is system bsdtar, `.zip` is fine.
  - Scope clean: one new file, `.github/workflows/rust_build.yml`; no Rust source touched; only
    other change is the `TASKS.md` archival edit.

  **PENDING MANUAL (user) CHECK — carried forward:**
  - [ ] Push a branch / `v*` tag and watch Actions: all three legs build + test green.
  - [ ] Linux apt list is sufficient for the full `cargo build --release` on a clean runner
        (nothing else wgpu/rfd needs at compile time).
  - [ ] `primewatch3-windows.zip` produced on the runner is a real zip (see reviewer flag above),
        and the macOS `.zip` / Linux `.tar.gz` unpack to `primewatch3/prime_defs/` + binary.
  - [ ] On a `v*` tag the single GitHub Release ends up with all three archives attached.

  **POST-REVIEW FIX (orchestrator, same `port(P10.1)` commit):**
  - The reviewer's non-blocking flag (Windows leg running `tar -a *.zip` under `shell: bash` could
    resolve GNU tar and silently emit a mislabeled non-zip) is now **resolved in code**, not left
    as a manual check. The single `shell: bash` package step was split into two mutually-exclusive
    steps:
    - `Create release archive (Windows)` — `if: runner.os == 'Windows'`, `shell: pwsh`,
      `New-Item` / `Copy-Item -Recurse` / `Compress-Archive -Path primewatch3 -DestinationPath
      primewatch3-windows.zip`. No dependency on which `tar` is on PATH.
    - `Create release archive (Unix)` — `if: runner.os != 'Windows'`, `shell: bash`,
      `mkdir` / `cp -r` / `tar -czf`.
  - macOS `archive` matrix var changed `primewatch3-macos.zip` → `primewatch3-macos.tar.gz` (the
    Unix step always produces a gzip tarball; bsdtar-on-mac zip is no longer used). Windows stays
    `.zip`, Linux stays `.tar.gz`.
  - The earlier "`tar -a -c -f` uniform across legs / bsdtar-on-Windows" notes above are
    superseded by this. `python3 -c "import yaml; yaml.safe_load(...)"` still parses; no Rust
    source touched.
