# P8.4.5 — archived completed task

- Task id: P8.4.5
- Status: archived completed task
- Commit: the commit that adds this file (`port(P8.4.5): ...`)
- Summary lives in [`../TASKS.md`](../TASKS.md); this file is the full historical entry.

---

- [x] **P8.4.5** Screen-space text overlays + `renderImGui` status windows + `EItemType` enum. — `DONE` · ports `WorldRenderer.cpp:408-529` (status windows), text overlay logic from draw fns, `EItemType.hpp/.cpp`, + screen-space projection helpers.

  **Steps:**
  1. [x] Port `EItemType` enum + `itemTypeToName` function as `src/defs/item_types.rs` (41 variants).
  2. [x] Port `GameObjectUtils::getAllLoadingDatas` to `src/mem/game_object_utils.rs` — walks `g_main["globalObjects"]["gameResFactory"]["loadList"]` chain.
  3. [x] Implement `glm::project` equivalent in renderer: `project(pos: Vec3, view: Mat4, projection: Mat4, viewport: [f32; 4]) -> Vec3` (NDC divide + viewport map). Store `cam_viewport: [f32; 4]` on `WorldRenderer`, set it in `resize` and `update` (post-camera-setup).
  4. [x] Add `pub struct TextOverlay { pub screen_pos: Vec2, pub text: String }` + `text_overlays: Vec<TextOverlay>` field to `WorldRenderer`. Add public method `clear_text_overlays()` + `add_text_overlay(screen_pos: Vec2, text: String)`.
  5. [x] Update P8.4.4 draw functions (`draw_bomb`, `draw_ai`, `draw_pickup`) to call `self.add_text_overlay(...)` instead of logging (modify their signatures from the already-ported versions to add the overlay calls, reusing `project` from step 3).
  6. [x] Add `pub fn render_status_windows(&self, ctx: &Ctx, ui: &mut egui::Ui)` method to `WorldRenderer` — renders two windows: "WorldStatus" (area table with `EChain`/`EPhase` enum lookups + loading datas) and "PlayerStatus" (pos/vel/look angle display from `self.player*` + `player_look_vec`). (C++ `WorldRenderer::renderImGui` lines 408-529 ported; can add call site in `app.rs` behind `defs_loaded` gate now or defer to P9.1 — recommend now for testability.)

  **Implementation notes (P8.4.5):**
  - New `src/defs/` module (`mod defs;` in `main.rs`, `src/defs/mod.rs`) — `defs::item_types` with `EItemType` (`#[repr(i32)]`, discriminants match C++ incl. `Invalid = -1` / `Max = 41`), `EItemType::from_raw(u32)` (unknown → `Invalid`, matching the C++ switch `default`), `item_type_to_name(EItemType) -> &'static str`.
  - `get_all_loading_datas(ctx) -> Vec<GameInstance>` in `mem/game_object_utils.rs`. Verified against `GameObjectUtils.cpp:107-127`: walks `g_main["globalObjects"]["gameResFactory"]["loadList"]` (templated `rstl::list<SLoadingData>` — `first`/`end`/`next` auto-deref `*rstl::list_node`, `item` is the inline `SLoadingData`). Loop: `while current.address != end.address && != 0`, emergency-exit on `res.len() > size`, push `item`, advance `next`. `get_main` added to the `mem::globals` import.
  - `renderer.rs`: `pub(crate) fn project(pos, view, proj, viewport) -> Vec3` (task's formula: `clip = proj*view*vec4(pos,1)`, `/w`, `screen.xy = vp.xy + (ndc.xy+1)*0.5*vp.zw`; returns raw NDC `z`, unused). `cam_viewport: [f32;4]` field set in `new`/`resize` (from target size) and every `update` (from `viewport_size`). `TextOverlay { screen_pos: Vec2, text: String }` + `pub text_overlays: Vec<TextOverlay>`, `clear_text_overlays()` (called first thing in `update`), `add_text_overlay(...)`.
  - Private `screenspace_pos_for_actor` / `screenspace_pos_for_physics_actor` (`WorldRenderer.cpp:912-941`) return `Option<Vec2>` with the Y-flip applied; the physics-actor one reuses the `collisionPrimitive → baseBoundingBox → renderBounds` ladder (`is_degenerate_bbox`, renderBounds `pos`-relative). `draw_bomb` queues the fuse-frame count, `draw_ai` the `healthInfo.health` (`{:.1}`), `draw_pickup` two lines (`"<item> <amt>/<cap>"` above by `OVERLAY_LINE_HEIGHT`, `"<cur>/<life>"` below).
  - `render_status_windows(&self, ctx, ui)`: spawns `WorldStatus` + `PlayerStatus` `egui::Window`s off `ui.ctx()`. WorldStatus = area `egui::Grid` (MREA/Chain/Phase/Occluded, skip `curChain == 1`, `EChain`/`EPhase` via `ctx.structs.get_enum_by_name` with numeric fallback, occlusion via `postConstructed.occlusionState == 1`) + the loading-data list (first 5 by tag+size, `+Xk = Yk` summary). PlayerStatus = pos/vel/look readout from `self.player*` + `self.player_look_vec`.
  - **Call site wired now** (per task recommendation): `app.rs` `render()` gained `ctx: Option<&Ctx>`; when defs are loaded it hosts `render_status_windows` inside an `egui::Area`. `WorldInput`/highlight plumbing unchanged.
  - **Deviations (sanctioned):** no font system in this layer, so C++ `centerX = CalcTextSize(...).x/2` horizontal centering and the `-TextLineHeight/2` nudge are dropped — `screen_pos` is the bare projected point; multi-line stacking uses a nominal `OVERLAY_LINE_HEIGHT = 14.0`. P9.1's overlay painter owns exact glyph metrics. `render_player_status` uses `Vec2::normalize_or_zero` where C++ uses raw `glm::normalize` (NaN on zero) — safer for a status readout. Loading-size accumulators use `saturating_add` vs C++ plain `u32` `+=`. `project` returns NDC `z` (glam DirectX [0,1] proj) rather than GL's `(z+1)*0.5`; no caller reads `.z`.
  - New tests: `item_types` (from_raw / names / discriminants), `game_object_utils` (`get_all_loading_datas` zeroed-empty + live-walk over `mem1.raw`), `renderer` (`project` centre/corner/perspective-divide, pickup overlay string format). `cargo clippy --all-targets` + `cargo test` clean (102 passed).

  **Port from:**
  - `WorldRenderer.cpp:912-941`: `getScreenspacePosForActor` / `getScreenspacePosForPhysicsActor` — read entity transform, project world pos to screen via MVP + viewport scale, flip Y.
  - `WorldRenderer.cpp:873-879`: `drawBomb` text overlay — calc fuse frames, center-align text, call `ImGui::GetBackgroundDrawList()->AddText()`.
  - `WorldRenderer.cpp:900-909`: `drawAi` text overlay — read health, center text, overlay.
  - `WorldRenderer.cpp:943-973`: `drawPickup` text overlay — read `itemType` / `amount` / `capacity` / `lifeTime` / `curTime`, render two lines.
  - `WorldRenderer.cpp:408-529`: `renderImGui` — two ImGui windows with docked layout. WorldStatus: `AreaUtils::getAreas()` loop, enum lookups for chain/phase, `getAllLoadingDatas` resource list. PlayerStatus: formatted pos/vel/look-direction display.
  - `EItemType.hpp:5-51` / `EItemType.cpp:3-90`: enum (41 values + Invalid/Max markers) + name switch.
  - `GameObjectUtils.cpp:107-127`: `getAllLoadingDatas` — walk linked list off `g_main["globalObjects"]["gameResFactory"]["loadList"]`.

  **Watch for:**
  - `glm::project(pos, view, proj, viewport)` is `(pos in NDC coords) = projection * view * pos; ndc /= ndc.w; screen = (viewport.xy + (ndc.xy + 1) * 0.5 * viewport.zw)` — standard OpenGL projection; Y flips in the overlay code.
  - `cam_viewport = [0, 0, width, height]` (NOT `[0, 0, 1, 1]` — it's pixel-space). Set in `resize` when target size changes, and re-confirm in `update` before project calls.
  - Text-overlay accumulation must clear at the start of each `update` (before drawing entities).
  - `EChain` / `EPhase` enums already exist in `.bs` files — use `ctx.structs.enum_by_name(...)` lookups (no `.unwrap()` — skip on `None`).
  - `getAllLoadingDatas` chain walk: `first = list["first"]`, loop while `current.address != end.address`, each iteration reads `current["item"]` then `current = current["next"]`. Stop on address==0 or overflow.
  - P8.4.4 draw functions are stubs in the current code — modify them to add the overlay calls (already ported as no-op stubs; now add the meat). Their signatures already exist; just add the overlay line(s).
  - No live display possible in this env — test with `cargo build` + read overlays from breakpoint inspection or add a debug `eprintln!` to verify accumulation.

  **Done when:**
  - `cargo build` + `cargo clippy --all-targets` clean.
  - `src/defs/item_types.rs` exists with enum + `fn item_type_to_name(EItemType) -> &'static str`.
  - `src/mem/game_object_utils.rs` exports `pub fn get_all_loading_datas(ctx: &Ctx) -> Vec<GameInstance>`.
  - `WorldRenderer` has `cam_viewport`, `text_overlays` field + `project` / `add_text_overlay` / `clear_text_overlays` / `render_status_windows` methods.
  - Draw functions (bomb/ai/pickup) call `add_text_overlay` with formatted text (verify via breakpoint or test read).
  - `render_status_windows` compiles and integrates (call site in `app.rs` near the existing World/status window, or deferred to P9.1 per reviewer choice).

  **Review (port-reviewer):** Walked `project`, `screenspace_pos_for_actor` / `_physics_actor`, `draw_bomb` / `draw_ai` / `draw_pickup` overlays, `render_world_status` / `render_player_status`, `get_all_loading_datas`, and `EItemType` side by side with `WorldRenderer.cpp:408-529` / `840-973`, `GameObjectUtils.cpp:107-127`, `EItemType.{hpp,cpp}` — behaviour matches (glm::project x/y mapping, Y-flip, bbox ladder incl. the collisionPrimitive-no-offset / renderBounds-minus-pos asymmetry, list-walk termination + emergency exit, all 41 item names + `default → "Unknown"`). All-`Option` reads bail instead of C++ total-read-0 — sanctioned by the P4.2/P5.1 convention, inert on a valid snapshot. `cam_viewport` / `cam_projection` / `cam_view` are all set before the draw dispatch; `clear_text_overlays` runs first in `update`. `cargo fmt --check` clean; `cargo test` 102 passed; `cargo clippy --all-targets` net −4 warnings (previously-dead fns now wired), no new lints. Three open questions accepted: `render_status_windows(&mut egui::Ui)` matches the task spec (only used for `ui.ctx()`); dropped glyph-metric centering is explicitly P9.1's; the loading-datas live test asserting only termination is adequate (queue empty on a settled snapshot).
  Manual-verification (needs live Dolphin + display): confirm the WorldStatus/PlayerStatus windows populate with sane area/chain/phase rows and player pos/vel/look, and that bomb/AI/pickup labels land on-screen near their entities once P9.1 paints `text_overlays`.

