# P8.4.4 — archived completed task

- Task id: P8.4.4
- Status: archived completed task
- Commit: the commit that adds this file (`port(P8.4.4): ...`)
- Summary lives in [`../TASKS.md`](../TASKS.md); this file is the full historical entry.

---

- [x] **P8.4.4** Projectile/bomb/powerBomb/ai/pickup/chozoGhost/collisionActor draw fns. — `DONE`

  **Implementation notes**

  - `src/world/renderer.rs`: filled in all seven P8.4.3 stubs.
    - `draw_ai` / `draw_pickup` reduce to `self.draw_physics_actor(...)` (C++
      remainder is P8.4.5 text).
    - `draw_power_bomb`: `curTime` window `1.0..=4.0` gate, sphere at `curRadius`
      on `translucent_render_buff` with `set_transform(transform)`, color
      `(0.8,0.4,0.4,0.4)`, no highlight branch.
    - `draw_bomb`: `bomb_fuse_frames` gate; **ignores** its `is_highlighted`
      param (named `_is_highlighted`) and recomputes via `bomb_proximity_highlight`
      against `self.player.position`; truncated sphere radius `1.5 - 0.7`.
      Skipped C++ 873-878 (HP text).
    - `draw_projectile`: `projectileActive` bitfield gate via `read_bool`; nested
      inline `CProjectileWeapon` (`entity["projectile"]`, offset 0x170, not a
      pointer); world pos/vel via new `projectile_world_pos` / `projectile_world_vel`
      helpers; world-space cube (`set_transform(IDENTITY)`) + one/two velocity
      lines. **Dropped the dead line-821 `transform` read** (never used in C++).
    - `draw_collision_actor`: axis cross on opaque `render_buff` with `solid_color`
      (`color.with_w(1.0)`); aabb→sphere→obbTreeGroup ladder keyed on
      auto-deref'd `.address != 0` of the `*Cx` primitive members; unknown →
      `eprintln!`. **Dropped the dead line-977 `pos`.** `CSphere` members
      confirmed `origin` / `radius` in `math/CSpherePrimitive.bs`.
    - `draw_chozo_ghost`: gained `objects: &HashMap<TUniqueID, GameInstance>`
      param, threaded through the single `render_entities` call site; `draw_ai`
      then magenta line ghost→cover (`coverPoint & 0x3FF` slot lookup). Dead
      `spaceWarpPosition` + commented warp line skipped.
  - New `pub(crate)` pure helpers with unit tests (7 new tests):
    `bomb_fuse_frames`, `bomb_proximity_highlight`, `projectile_world_pos`,
    `projectile_world_vel`.
  - Followed the P8.4.3 `None`→skip-the-draw convention throughout (no
    `unwrap_or_default`).
  - `cargo fmt`: clean. `cargo clippy --all-targets`: exit 0, no new warnings
    (pre-existing `collision_mesh.rs` / `prime_structs.rs` lints from P4.1/P8.1
    untouched; dead-code warnings for the seven draw fns are now gone).
  - `cargo test`: `94 passed; 0 failed`.

  Fill in the seven `&mut self` no-op stubs in `src/world/renderer.rs`
  (`draw_projectile` / `draw_bomb` / `draw_power_bomb` / `draw_chozo_ghost` /
  `draw_pickup` / `draw_collision_actor` / `draw_ai`) with the real geometry.
  **No text/screen-space overlays this phase** — every `ImDrawList` / `AddText` /
  `getScreenspacePosFor*` block is P8.4.5; skip it. All CPU accumulation stays in
  `update` (via `render_entities`), same as the P8.4.3 draw fns.

  **Steps**

  1. `draw_ai` — port `WorldRenderer.cpp:898-910`. With the text overlay dropped,
     the body is just `self.draw_physics_actor(ctx, entity, is_highlighted)`
     (the C++ `healthInfo/health` read + `AddText` are P8.4.5). Keep the fn so the
     dispatch chain and `draw_chozo_ghost` can call it.

  2. `draw_pickup` — port `WorldRenderer.cpp:943-973`. Same: text-only C++ fn, so
     the body reduces to `self.draw_physics_actor(ctx, entity, is_highlighted)`.
     Do **not** port `itemTypeToName` / `EItemType` yet (P8.4.5).

  3. `draw_power_bomb` — port `WorldRenderer.cpp:881-896`. `curTime =
     entity["curTime"].read_f32()`; bail if `curTime < 1.0 || curTime > 4.0`.
     `curRadius = entity["curRadius"].read_f32()`. `transform =
     readAsCTransform(entity["transform"])`. `color = {0.8,0.4,0.4,0.4}`
     (no highlight branch). `translucent_render_buff`: `set_color`,
     `set_transform(transform)`, `add_tris(generate_sphere(Vec3::ZERO, cur_radius,
     color))`. (`CPowerBomb : CWeapon`, `curTime` 0x15C / `curRadius` 0x160.)

  4. `draw_bomb` — port `WorldRenderer.cpp:844-879` **minus 873-878** (the
     `ImDrawList` HP text). `fuse_time = entity["fuseTime"].read_f32()`;
     `fuse_frames = (fuse_time * 60.0).ceil() as i32 + 1`; bail if
     `fuse_frames <= 0`. `transform = readAsCTransform(entity["transform"])`;
     `pos = transform.w_axis.truncate()`. `pos_to_ball = self.player.position +
     Vec3::new(0,0,0.7) - pos`. `max_distance = 1.5`. Highlight is
     **recomputed** here: `is_highlighted = pos_to_ball.length() < max_distance
     && pos_to_ball.z >= -0.7` (ignores the passed-in flag — verbatim C++).
     `color = if is_highlighted {(0.8,0,0,0.8)} else {(0.7,0.5,0.5,0.5)}`.
     `translucent_render_buff`: `set_color`, `set_transform(transform)`,
     `add_tris(generate_truncated_sphere(Vec3::ZERO, max_distance - 0.7, 0.0,
     color))`. (`CBomb : CWeapon`, `fuseTime` 0x17C.)

  5. `draw_projectile` — port `WorldRenderer.cpp:800-842`.
     `if !entity["projectileActive"].read_bool() { return }` (bitfield
     `0x2e4:7:1` — `read_bool` handles it). `projectile = entity["projectile"]`
     (inline `CProjectileWeapon` at 0x170, **not** a pointer).
     `local_to_world = readAsCTransform(projectile["localToWorldXf"])`,
     `local_xf = readAsCTransform(projectile["localXf"])` (both `Mat4`).
     `proj_off/local_off/world_off = readAsCVector3f(projectile["projOffset" /
     "localOffset" / "worldOffset"])` — extended to `Vec4(_, 0.0)` for the matrix
     math. `pos = (local_to_world * (local_xf * proj_off4 + local_off4) +
     world_off4).truncate()`. `scale = readAsCVector3f(projectile["scale"])`.
     `vel = (local_to_world * local_xf * Vec4(velocity,0)).truncate()` where
     `velocity = readAsCVector3f(projectile["velocity"])`.
     `extent = entity["projExtent"].read_f32()` (0x2A4).
     `size = Vec3::splat(extent) / 2.0 * scale`; `min = pos - size`,
     `max = pos + size`. `color = if is_highlighted {(1,0,0,0.5)} else
     {(0.8,0.4,0.4,0.8)}`. `translucent_render_buff`: `set_color(color)`,
     `set_transform(Mat4::IDENTITY)`, `add_tris(generate_cube(min, max, color))`.
     Then: `if is_highlighted { set_color((0.8,0.8,0.8,0.5));
     add_line(pos, pos + vel.normalize() * 1000.0) }`; always
     `set_color((1,0.5,0.5,1)); add_line(pos, pos + vel.normalize() * 0.5)`.
     The C++ `transform = readAsCTransform(entity["transform"])` at line 821 is
     dead — **drop it** (deviation, note it).

  6. `draw_collision_actor` — port `WorldRenderer.cpp:975-1023`.
     `transform = readAsCTransform(entity["transform"])` (the C++ `pos =
     transform[3]` at line 977 is dead — drop it). `color = if is_highlighted
     {(1,0,0,0.5)} else {(1,1,1,0.5)}`; `solid_color = color` with `.w = 1.0`.
     Resolve the three pointer primitives via `entity.get_member(ctx, name)` and
     test **non-null** the same way `draw_actor` tests `modelData`:
     `entity.get_member(ctx, "aabbPrimitive").map_or(0, |m| m.address) != 0`
     (they're `*Cx` members — auto-deref makes `.address` the pointee, 0 if
     null). Axis cross goes on the **opaque** `render_buff` with `solid_color` +
     `set_transform(transform)`: three `add_line` pairs at ±0.2 on each axis.
     Then the `if / else if / else if / else` ladder on
     aabbPrimitive → spherePrimitive → obbTreeGroupPrimitive:
     - aabb: `min/max = readAsCVector3f(aabbPrimitive["aabb"]["min"|"max"])`,
       `translucent_render_buff.set_color(color)` + `set_transform(transform)` +
       `add_tris(generate_cube(min, max, color))`.
     - sphere: `center = readAsCVector3f(spherePrimitive["sphere"]["origin"])`,
       `radius = spherePrimitive["sphere"]["radius"].read_f32()`,
       `add_tris(generate_sphere(center, radius, color))` (translucent).
     - obbTreeGroup: `min/max =
       readAsCVector3f(obbTreeGroupPrimitive["container"]["aabb"]["min"|"max"])`,
       `render_buff.add_lines(generate_cube_lines(min, max, color))` (opaque,
       `solid_color` set).
     - else: `eprintln!("Uhoh! unknown collision actor!")`.
     Set `translucent_render_buff` color/transform once before the ladder (C++
     lines 993-994) so branches that only push tris inherit it.
     Confirm `CSphere` has `origin` / `radius` members in the `.bs` before
     using those names; adjust to the actual member names if they differ.

  7. `draw_chozo_ghost` — port `WorldRenderer.cpp:775-798`. **Signature change:**
     add a `objects: &HashMap<TUniqueID, GameInstance>` param and pass
     `objects` at the `render_entities` call site (`self` is `&mut`, `objects`
     `&` — no borrow conflict with the `for entity in objects.values()` loop).
     Body: `self.draw_ai(ctx, ghost, highlighted)` first. Then
     `ghost_pos = readAsCTransform(ghost["transform"]).w_axis.truncate()`.
     `cover_point_id = (ghost["coverPoint"].read_u16() & 0x3FF)` (0x674).
     `if let Some(cover) = objects.get(&cover_point_id)`:
     `cover_pos = readAsCTransform(cover["transform"]).w_axis.truncate()`,
     `translucent_render_buff.set_transform(Mat4::IDENTITY)`,
     `set_color((1,0,1,1))`, `add_line(ghost_pos, cover_pos)`.
     The C++ `spaceWarpPosition` read (779) + commented-out line 783-785 are
     dead — skip. (`objects` is keyed by the same slot id `coverPoint & 0x3FF`
     resolves to — the P6.1 map key is the `CObjectList` slot index.)

  8. Helper: add a `read_transform_at(ctx, inst, path)` alongside the existing
     `read_vec3_at` (`walk_member` + `read_as_transform`) if it tidies the
     projectile/collision-actor chains; not required.

  9. Follow the P8.4.3 `None` convention: any missing member / OOB read → `let
     Some(x) = … else { return }` (skip the draw), never `unwrap_or_default`.
     Bitfield/`read_bool`/`read_f32`/`read_u16` on a valid snapshot never hit
     this.

  10. Add `#[cfg(test)]` coverage for the pure bits that don't need a GPU:
      the bomb `fuse_frames` / proximity-highlight predicate and the projectile
      `pos` / `vel` transform math are good candidates — factor them into
      `pub(crate)` free fns (like `physics_actor_bbox` / `player_speed_color`)
      and unit-test against hand-computed values.

  **Port from**

  - `../primewatch2/src/world/WorldRenderer.cpp:775-798:WorldRenderer::drawChozoGhost`
    — drawAi + coverPoint line; skip `spaceWarpPosition` (dead) and the ImGui.
  - `:800-842:WorldRenderer::drawProjectile` — `projectileActive` gate; nested
    `CProjectileWeapon` transform/offset chain → world `pos` + `vel`; cube +
    two velocity lines. Drop the dead line-821 `transform` read.
  - `:844-879:WorldRenderer::drawBomb` — fuse-frame gate; ball-proximity
    highlight recompute; truncated sphere. **Skip 873-878** (HP text).
  - `:881-896:WorldRenderer::drawPowerBomb` — `curTime` window gate; sphere at
    `curRadius`.
  - `:898-910:WorldRenderer::drawAi` — reduces to `drawPhysicsActor` (rest is
    P8.4.5 text).
  - `:943-973:WorldRenderer::drawPickup` — reduces to `drawPhysicsActor` (rest
    is P8.4.5 text).
  - `:975-1023:WorldRenderer::drawCollisionActor` — axis cross on opaque buffer;
    aabb/sphere/obbTreeGroup primitive ladder; unknown → stderr. Drop the dead
    line-977 `pos`.
  - `../primewatch2/src/world/WorldRenderer.cpp:583-662:WorldRenderer::renderEntities`
    — for the `draw_chozo_ghost` call-site signature (`entities` map is passed
    through).
  - `src/mem/math_utils.rs` (`read_as_vec3` / `read_as_transform`) — the readers;
    `src/gl/shapes.rs` (`generate_cube` / `generate_sphere` /
    `generate_truncated_sphere` / `generate_cube_lines`) — the geometry;
    `src/gl/immediate.rs` (`ImmediateModeBuffer::{set_color,set_transform,add_tris,add_lines,add_line}`).
  - `prime_defs/prime1/entities/{CGameProjectile,CBomb,CPowerBomb,CChozoGhost,CCollisionActor}.bs`
    — member names/offsets (`CProjectileWeapon` / `CCollidableAABox` /
    `CCollidableSphere` / `CCollidableOBBTreeGroup` live in `CCollisionActor.bs`).

  **Watch for**

  - `set_color` takes `[f32; 4]` (use `color.to_array()`); `set_transform` takes
    `Mat4`. `generate_*` take `Vec3`/`Vec4` and return `Vec<Vert>` — pass
    `&verts` to `add_tris` / `add_lines`.
  - `draw_bomb` **ignores** its `is_highlighted` param — the proximity test
    overrides it. Keep the param (dispatch passes it) but shadow it.
  - `generate_truncated_sphere` / `generate_sphere` take a **center** arg — C++
    passes `{0,0,0}` and relies on `set_transform(transform)` to place it. Match
    that: `Vec3::ZERO` + `set_transform`, don't bake `pos` into the center.
  - `draw_projectile` uses `set_transform(Mat4::IDENTITY)` for the cube (world
    space) — the min/max are already world-space.
  - `Vec3::splat(extent) / 2.0 * scale` — component-wise; glam `Vec3 * Vec3` is
    Hadamard, matches C++ `glm::vec3 * glm::vec3`.
  - `Mat4 * Vec4` in glam returns `Vec4`; `.truncate()` → `Vec3`. C++
    `glm::vec3 = glm::vec4` truncates silently.
  - `draw_chozo_ghost` new param ripples only to the one `render_entities`
    call site.
  - clippy `--all-targets` + `cargo test` clean; `cargo fmt` (2-space).

  **Done when** `cargo clippy --all-targets` + `cargo test` are clean, the seven
  draw fns render their C++ geometry (no overlays), `draw_chozo_ghost` takes the
  `objects` map, and new pure helpers have unit coverage. Then move to `IN REVIEW`.

  **Review**

  Verified `src/world/renderer.rs` against `WorldRenderer.cpp:775-1023` and
  `renderEntities` 583-662 function-by-function. All seven draw fns are
  behaviour-faithful:
  - Member names/offsets confirmed in `prime_defs/prime1`: `CGameProjectile` /
    `CProjectileWeapon` in `entities/CWeapon.bs` (`projectileActive 0x2e4:7:1`,
    `projExtent 0x2A4`, inline `projectile 0x170`, and every
    `localToWorldXf`/`localXf`/`projOffset`/`localOffset`/`worldOffset`/`scale`/`velocity`);
    `CBomb.fuseTime 0x17C`; `CPowerBomb.curTime 0x15C`/`curRadius 0x160`;
    `CChozoGhost.coverPoint 0x674` (`u16`); `CCollisionActor` pointer primitives
    `aabbPrimitive`/`spherePrimitive`/`obbTreeGroupPrimitive`; `CAABB.min/max`,
    `CSphere.origin 0x0`/`radius 0xC`, OBB `container->aabb`.
  - `draw_bomb` correctly shadows `_is_highlighted` and recomputes via
    `bomb_proximity_highlight(self.player.position, pos)`; HP text (873-878)
    skipped.
  - `draw_chozo_ghost` signature gained `objects`, threaded through the single
    `render_entities` call site; no borrow conflict.
  - Dead reads dropped as directed: projectile `transform` (821), collision-actor
    `pos` (977).
  - Geometry placement matches C++: projectile cube in world space
    (`Mat4::IDENTITY`, world-space min/max), bomb/power-bomb spheres at
    `Vec3::ZERO` + `set_transform(transform)`.
  - `.bs`-sourced offsets only; no new globals; `Ctx` threaded; `None`->skip
    convention followed throughout; 2-space indent.

  `cargo fmt --check`: clean. `cargo test`: 94 passed. `cargo clippy
  --all-targets`: no warnings touching `renderer.rs` (pre-existing dead-code /
  collapsible-if lints unchanged).

  Nit (optional, non-blocking): `draw_power_bomb`'s `!(1.0..=4.0).contains(&t)`
  bails on `NaN` where C++ `t < 1.0 || t > 4.0` would draw — irrelevant on a
  valid snapshot.

  Not verifiable without a live Dolphin + display: the actual on-screen geometry.
  Pure transform/gate math is unit-covered (7 new tests).
