# P8.4.1 — archived completed task

- **Task:** P8.4.1 Port `MathUtils` + `GameInstance::extends_class` — glam vector/matrix readers & type checks
- **Status:** DONE
- **Commit:** carried by the commit that adds this file (`port(P8.4.1): MathUtils glam readers + GameInstance::extends_class`)
- **Summary lives in:** [`TASKS.md`](../TASKS.md) (Phase 8 · P8.4)

Full task entry preserved verbatim below.

---

- [ ] **P8.4.1** Port `MathUtils` + `GameInstance::extends_class` — glam vector/matrix readers & type checks. — `IN REVIEW`
  - **Steps:**
    1. [x] Create `src/mem/math_utils.rs` with four glam reader functions: `read_as_vec3`, `read_as_quat`, `read_as_matrix4f`, `read_as_transform`.
    2. [x] Each function takes `&Ctx` + `&GameInstance` (member) and returns `Option<glam_type>`, following the C++ MathUtils signatures.
    3. [x] For `read_as_matrix4f` and `read_as_transform`, route through the `member["matrix"]`/`member["m0"]` accessor pattern and column-major offset arithmetic.
    4. [x] Add `pub mod math_utils;` to `src/mem/mod.rs`.
    5. [x] Add `GameInstance::extends_class(&self, ctx: &Ctx, class_name: &str) -> bool` method in `src/structs/prime_structs.rs` — delegates to `GameStruct::extends`.
    6. [x] Add unit tests for each reader with `mem1.raw` spot-checks (same pattern as `game_memory.rs` tests).
    7. [x] Verify `cargo build` + `cargo clippy --all-targets` + `cargo test` all pass.
  - **Port from:**
    - `../primewatch2/src/utils/MathUtils.cpp:4-10:readAsCVector3f` — reads member["x"/"y"/"z"] as three f32s, packs into glm::vec3.
    - `../primewatch2/src/utils/MathUtils.cpp:12-19:readAsCQuaternion` — reads member["x"/"y"/"z"/"w"] as four f32s, packs into glm::quat (order: x,y,z,w).
    - `../primewatch2/src/utils/MathUtils.cpp:21-49:readAsCMatrix4f` — reads 16 floats from member["matrix"] using column-major (r + c*4)*4 offset math, builds glm::mat4 (column-major layout: data[0..4] = col0, data[4..8] = col1, etc.).
    - `../primewatch2/src/utils/MathUtils.cpp:51-79:readAsCTransform` — reads 12 floats from member["m0"] with same offset math, builds glm::mat4 with bottom row [0,0,0,1].
    - `../primewatch2/src/defs/GameDefinitions.cpp:227-231:GameMember::extendsClass` — member checks if its own type or any parent extends the target class name.
  - **Watch for:**
    - Both glam 0.33 matrix constructors are column-major (opposite of C++ glm default row-major); verify the data order matches the C++ output byte-for-byte.
    - Carry-over bug already fixed in Phase 4: `GameStruct::extends` now recurses correctly.
    - `read_as_transform` w component is hardcoded 1.0 (not read from memory) — match C++ line 75 exactly.
    - `None` on any intermediate read (missing member, OOB) returns `None` (no `unwrap_or_default`); deferred to P8.4.2+ call sites.
  - **Done when:**
    - `cargo build` clean; `cargo clippy --all-targets` has no new warnings.
    - `cargo test src/mem/math_utils.rs` + `cargo test src/structs/prime_structs.rs` pass all tests (including spot checks vs. mem1.raw if present).
    - `GameInstance::extends_class(ctx, "CGameCamera")` correctly resolves transitive inheritance (verify via unit test with a hand-built struct chain).
  - **Implementation notes:**
    - `src/mem/math_utils.rs` (new): 4 free fns `read_as_vec3/quat/matrix4f/transform(ctx: &Ctx, member: &GameInstance) -> Option<glam>`. Vec3/Quat compose `member.get_member(ctx, "x")?.read_f32(ctx)?`; Quat packs `(x,y,z,w)` via `Quat::from_xyzw` matching the C++ `glm::quat{x,y,z,w}` brace-init order. Matrix readers take `member.get_member(ctx, "matrix"/"m0")?.address` and read raw f32s at `base + (r + c*4)*4` (the C++ `RC` macro), feeding the 16 values in the *same arg order* to `Mat4::from_cols_array` — glam's array is column-major, matching what C++ hands to `glm::mat4`, so the byte layout is identical. `read_as_transform` hardcodes the 4th element of cols 0-2 to `0.0` and col 3 `w` to `1.0` (not read), per C++ lines 60/65/70/75.
    - Deviation from C++ (sanctioned by task Watch-for + P4.2/P8.1 precedent): all reads are fallible — a missing sub-member or OOB address yields `None`, no `unwrap_or_default`/panic. Deferred to P8.4.2+ call sites.
    - `GameInstance::extends_class` added to `src/structs/prime_structs.rs` — exact port of `GameMember::extendsClass` (`GameDefinitions.cpp:227-232`): identity check, then `get_type(ctx).map(|s| s.extends(ctx.structs, class_name))`. Uses the already-fixed (P4.1) `GameStruct::extends` recursion. Unknown type name matches only on identity.
    - Tests: `math_utils` has 7 (synthetic BE memory + hand-built structs for vec3/quat/column-major matrix/transform hardcoded-column/missing-member-None/OOB-None, plus a byte-layout check vs `../primewatch2/mem1.raw` treating the disc header as a float block — skip-if-absent). `prime_structs` gains `extends_class_resolves_transitive_inheritance` (hand-built `C:B:A` chain + unknown-type case). C++ `MathUtils.cpp` and `GameDefinitions.cpp:227-232` were the only C++ read; the planned line ranges were accurate.
    - No `mem1.raw` semantic spot-check (no known live `CTransform`/`CMatrix4f` address without wiring globals — the byte-layout check covers offset arithmetic against real file bytes instead).
    - `cargo fmt`: clean. `cargo build`: clean. `cargo clippy --all-targets`: +4 warnings vs baseline (94→98), all `function ... is never used` for the 4 readers — dead until P8.4.2 wires them, no `#[allow]` per the P8.1-8.3 precedent. `extends_class` folds into the existing `GameInstance` "associated items never used" group. `cargo test`: 74 passed, 0 failed.

  - **Review:** Walked all four readers side-by-side with `MathUtils.cpp:4-79`. Quat field order `(x,y,z,w)` via `Quat::from_xyzw` matches the C++ `glm::quat{x,y,z,w}` brace-init. The `rc(r,c) = (r + c*4)*4` closure and the 16/12 arg positions into `Mat4::from_cols_array` reproduce the C++ `glm::mat4(...)` column-major fill exactly: glam `(col c, row r)` = C++ `(col c, row r)` = `raw[c + r*4]` for both. `read_as_transform` col0-2 4th element `0.0` and col3 `w` `1.0` (not read) match C++ lines 60/65/70/75; verified against the real `.bs` (`CTransform` = `CVector3f m0 / posX / m1 / posY / m2 / posZ` → col3 = posX/posY/posZ/1). Schema has `matrix` (`CMatrix4f`) and `m0` (`CTransform`) members, so the accessor path resolves. `member["matrix"].offset` (C++, absolute after `memberByName`) == Rust `.address`; per-cell reads route through `GameMemory::read_f32` (BE once, `& 0x7FFFFFFF` mask in `address_to_offset`). `extends_class` is an exact port of `GameMember::extendsClass` — identity check on own type, then delegate to `GameStruct::extends` (== C++ `GameStruct::extendsClass`, uses the P4.1 recursion fix); unknown type → identity-only. Fallible-read deviation is sanctioned. No new globals; `Ctx` threaded. 2-space indent, `cargo fmt --check` clean. `cargo test` 74 passed / 0 failed. Build + clippy trusted from untouched handoff (+4 expected dead-code warnings, P8.1-8.3 precedent). Scope clean (`TASKS.md`, `src/mem/mod.rs`, `src/structs/prime_structs.rs`, new `src/mem/math_utils.rs`).
