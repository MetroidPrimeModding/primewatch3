# P5.1 — archived completed task

Archived completed task. Carried by the commit that adds this file (`port(P5.1): …` on
`rust-conversion`). Summary lives in [`TASKS.md`](../TASKS.md); this file is the full entry.

---

- [x] **P5.1** Extend `src/mem/globals.rs` with the remaining globals (`gp_MemoryCard`,
  `gp_TweakPlayer`). — `DONE`

  **Port from:** `../primewatch2/src/defs/GameOffsets.hpp:1-10` (all four `GameMember` globals) +
  `../primewatch2/src/defs/GameObjectRenderers.cpp:34-42` (`render(m, addTree, derefPointer)` —
  the pointer-deref path: `uint32_t ptr = GameMemory::read_u32(member.offset); ptrMember.offset =
  ptr; ptrMember.pointer = false;`).

  Behavioral spec: `g_stateManager` / `g_main` are plain `GameMember`s whose `.offset` is the live
  address. `gp_MemoryCard` (`0x805a8c44`) and `gp_TweakPlayer` (`0x805a8cd8`) have `.pointer=true`
  — a `u32` pointer stored *at* the fixed address; the app derefs them at render time via the
  `derefPointer` arg.

  **Steps:**
  - [x] 1. Keep `get_state_manager()` / `get_main()` returning `GameInstance` unchanged.
  - [x] 2. Add `get_memory_card(ctx: &Ctx) -> Option<GameInstance>` and
     `get_tweak_player(ctx: &Ctx) -> Option<GameInstance>`: `ctx.mem.read_u32(FIXED_ADDR)?` then
     `GameInstance::new(addr, "CMemoryCardSys"/"CTweakPlayer")`. `Option` because the deref can hit
     unreadable memory (C++ `read_u32` is total via `getRealPtr` clamp; the Rust `GameMemory`
     returns `None` OOB — P3.1 deviation).
  - [x] 3. `use crate::ctx::Ctx;` added to `globals.rs`.
  - [x] 4. Tests: non-pointer roots match the `.hpp` constants; pointer roots deref (zeroed mem →
     `Some(addr=0)`, not `None`); skip-if-absent `mem1.raw` test asserts both live pointers land in
     the `0x8xxxxxxx` RAM window.

  **Watch for:**
  - `.rustfmt.toml` 2-space indent; `cargo clippy --all-targets` + `cargo test` clean.
  - Address masking is `GameMemory::read_u32`'s job (`& 0x7FFFFFFF` via `address_to_offset`) — don't
    pre-mask here.
  - `.bs` names must be exact: `CMemoryCardSys`, `CTweakPlayer` (both added in P0.1).

  **Done when:** both pointer globals resolve through a per-call `&Ctx` deref; roots unchanged;
  clippy + test green.

  **Implementation notes (P5.1):**
  - `src/mem/globals.rs` rewritten with a module doc comment explaining the pointer/non-pointer
    split. `get_state_manager` / `get_main` unchanged (still context-free, still `GameInstance`).
  - New `get_memory_card` / `get_tweak_player` take `&Ctx`, return `Option<GameInstance>`. Fixed
    addresses `0x805A8C44` / `0x805A8CD8` from `GameOffsets.hpp`.
  - Deviation from C++: pointer deref is fallible (`?` on `read_u32`) instead of clamp-to-0, matching
    the established P3.1/P4.2 "`Option`, no default substitution" convention. A null/zero pointer is
    **not** a failure — it derefs to `GameInstance` at address 0 (only OOB / unreadable yields `None`).
  - 3 tests added under `mod tests`. `mem1.raw` present in this env, so the live-dump test ran: both
    globals point into `0x8xxxxxxx`.
  - `cargo test`: 21 passed / 0 failed. `cargo clippy --all-targets`: +2 dead-code warnings
    (`get_memory_card` / `get_tweak_player` never used) — consistent with the rest of the not-yet-wired
    defs layer (cleared when P6/P7 call them). `cargo fmt` clean.

  **Review:** Verified against `GameOffsets.hpp` — the two fixed addresses, type names, and
  `.pointer=true` semantics all match; the deref mirrors `GameObjectRenderers.cpp:34-42`
  (`read_u32(offset)` → new handle at that address, pointer flag cleared). Non-pointer roots
  untouched. `Option` return + fallible deref is the sanctioned convention, not a C++ divergence in
  observable behavior for in-range reads. `cargo test` 21/21; clippy warnings are the expected
  not-yet-wired dead-code set.
