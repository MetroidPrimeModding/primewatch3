# P8.4.2 - archived completed task

- Task id: **P8.4.2**
- Status: **DONE** (archived)
- Commit: the commit that adds this file (`port(P8.4.2): ...`)
- Summary and forward-deps live in [`TASKS.md`](../TASKS.md) under `### P8.4 - WorldRenderer`.

---

- [x] **P8.4.2** `WorldRenderer` skeleton + camera modes + `updateAreas` + collision-mesh GPU upload. — `DONE`
  - **Goal:** stand up `src/world/renderer.rs` `WorldRenderer` as the real replacement for the P1.3 `SpikeScene` (the P1.3 contract: "`WorldRenderer::render` drops into `SpikeScene::render`'s place"). Camera math for all 3 modes, the `mesh_by_mrea` collision-mesh cache + GPU upload/draw, area AABB + camera-frustum line drawing, wired into `app.rs`. **No entities** (`renderEntities` / `drawPlayer` / the per-category draw fns are P8.4.3+).
  - **Steps:**
    1. New `src/world/renderer.rs` (`pub mod renderer;` in `src/world/mod.rs`). Port the enums `CullType {Back,Front,None}`, `CameraMode {FollowPlayer,Detached,GameCam}`, `OrbitPlayerCameraOrigin {Top,Center,Bottom}` and struct `GameCamera { perspective: Mat4, transform: Mat4, fov/znear/zfar/aspect: f32 }` (`WorldRenderer.hpp:19-44`). Add `pub struct WorldInput { cam_pitch: f32, cam_yaw: f32, cam_zoom: f32 }` (ports `PrimeWatchInput.hpp` minus `capturedMouse`; real winit plumbing is P9.1 — `app.rs` passes `WorldInput::default()` for now).
    2. `WorldRenderer` struct: pub camera params with the `WorldRenderer.hpp:83-113` defaults (`aspect 0`, `fov 45`, `z_near 0.1`, `z_far 10000`, `yaw 0`, `pitch 0.3`, `distance 10`, `up (0,0,1)`, `manual_camera_pos`, `light_dir (0.1,0.2,0.9)`, `culling: Back`, `camera_mode: FollowPlayer`, `orbit_player_camera_origin: Center`, `cam_line_length 10`); cached per-frame `cam_projection/cam_view: Mat4`, `cam_eye: Vec3`, `game_cam: GameCamera`, `player_pos/player_velocity/player_orientation(Quat)/player_is_morphed(bool)`, `last_known_non_colliding_pos: Vec3`, `player_look_vec: Vec3`; GPU state: offscreen `color`/`depth` `TextureView` + `size`, `WorldPipelines`, an `ImmediateModeBuffer` pair (`render_buff` / `translucent_render_buff`) + a `DynamicMesh` set for tris/lines × opaque/translucent, `mesh_by_mrea: HashMap<u32, CollisionMesh>` (CPU), `gpu_mesh_by_mrea: HashMap<u32, DynamicMesh>`.
    3. `WorldRenderer::new(device, size)` — build targets (reuse `gl::WORLD_COLOR_FORMAT` / `gl::WORLD_DEPTH_FORMAT`; **delete `src/scene.rs` and the `mod scene;` line** and fold its `create_targets` helper here — P8.2 forward-note "P8.4 unifies scene.rs's format consts"), `WorldPipelines::new`, the 2 `ImmediateModeBuffer`s (`init`, `WorldRenderer.cpp:115-118`), the 4 `DynamicMesh`es.
    4. `WorldRenderer::resize(device, size) -> bool` — same shape as `SpikeScene::resize` (recreate targets on change, return whether recreated).
    5. `WorldRenderer::color_view(&self) -> &wgpu::TextureView`.
    6. `WorldRenderer::update(&mut self, ctx: &Ctx, input: &WorldInput, viewport_size: (u32,u32))` — ports `WorldRenderer.cpp:120-168` + the camera-setup block `248-310` + the CPU-side geometry accumulation from `312-334` / `371-379` (the mesh-loop AABB lines only):
       - `update_areas` first (see step 7).
       - `pitch += input.cam_pitch; yaw += input.cam_yaw; distance += input.cam_zoom`; clamp `pitch` to `±(PI/2 - 0.1)`, `distance` to `[1, 100]` (`cpp:123-128`).
       - `aspect = viewport_size.0 / viewport_size.1` (C++ sets `aspect` from the framebuffer elsewhere; here derive it in `update`).
       - Player reads off `get_state_manager().get_member(ctx,"player")` (`*CPlayer` auto-deref): `transform` (→ `read_as_transform`, `player_pos = col3.xyz`), `velocity` (→ `read_as_vec3`), `lastNonCollidingState.translation` (→ `read_as_vec3` → `last_known_non_colliding_pos`), `lookDir` (→ `read_as_vec3` → `player_look_vec`), `orientation` (→ `read_as_quat`), `morphState` (`read_u32(ctx) == 1`). **Verify each member name against the real `.bs`** (`CPlayer` extends `CPhysicsActor` extends `CActor`; `CPlayer.lookDir 0x500` and `morphState 0x2F8` confirmed present; `transform`/`velocity`/`orientation` are inherited — grep `CActor.bs` / `CPhysicsActor.bs`; `lastNonCollidingState` is `CMotionState lastNonCollidingState 0x1F4` on `CPhysicsActor` — check `CMotionState.bs` for `translation`). Each read is `Option`; on `None` **keep the previous value** (do not zero — a mid-load miss shouldn't teleport the camera).
       - Camera manager (`cpp:141-150`): `sm.get_member(ctx,"cameraManager")` (`*CCameraManager`) → `curCameraId` (`read_u16`, but the `.bs` is `i16 curCameraId 0x0` — read via `read_u16` per C++), `camera = get_object_by_entity_id(ctx, cam_id)`, then **retype it to `"CGameCamera"`** (C++ `camera.type = "CGameCamera"` — set `camera.type_name` on the returned `GameInstance`, it's a `pub` field). Read `perspectiveMatrix` (→ `read_as_matrix4f`), `transform` (→ `read_as_transform`), `fov`/`znear`/`zfar`/`aspect` (`read_f32`) into `game_cam`. `.bs`: `CGameCamera { CMatrix4f perspectiveMatrix 0xEC, f32 fov 0x184, f32 znear 0x160, f32 zfar 0x164, f32 aspect 0x168 }`; `transform` inherited from `CActor`. All `Option` → keep-previous on miss.
       - Camera-setup block (`cpp:258-310`):
         - `FollowPlayer`: `cam_projection = perspective(fov, aspect, z_near, z_far)`; `angle = quat_from_euler(0, pitch, yaw)` matching glm's `glm::quat(glm::vec3(0,pitch,yaw))` (see Watch-for); `look_pos = last_known_non_colliding_pos` + the `orbit_player_camera_origin` z-nudge (`Top`: +1.4 morphed / +2.7 not; `Center`: +0.7 / +1.35; `Bottom`: +0). `cam_eye = look_pos - (angle * vec3(distance,0,0))`; `cam_view = look_at(cam_eye, look_pos, up)`; `manual_camera_pos = look_pos`.
         - `GameCam`: `cam_projection = game_cam.perspective`; `cam_view = game_cam.transform.inverse()`.
         - `Detached`: like `FollowPlayer` but orbiting `manual_camera_pos` (no z-nudge).
         - Then set `cam_eye = cam_view.inverse().w_axis.truncate()` for all modes (replaces the `glm::decompose` call at `cpp:297-310` — only `cam_eye` is consumed downstream this phase; `camPointing` is P8.4.5).
       - **Perspective fn:** reuse `glam::camera::rh::proj::directx::perspective(fov_radians, aspect, near, far)` + `glam::camera::rh::view::look_at_mat4` (same as deleted `scene.rs`). **Port the C++ verbatim:** C++ passes `fov` (default `45`) straight to `glm::perspective`, which expects **radians** — pass `self.fov` unconverted and add a `// NOTE:` that this looks like a latent C++ degrees/radians bug; flag it in the manual checklist rather than silently "fixing" it.
       - CPU geometry into the immediate buffers (`cpp:321-334`): `render_buff.clear()` + `translucent_render_buff.clear()`; `translucent_render_buff.set_transform(Mat4::from_translation(last_known_non_colliding_pos))` then `add_tris(shapes::generate_cube((-0.5,-0.5,0),(0.5,0.5,2.7),(1,0.5,0.5,0.5)))`; `render_buff.set_transform(Mat4::IDENTITY)` then `add_lines(shapes::generate_camera_line_segments(game_cam.perspective, game_cam.transform, cam_line_length))`.
    7. `WorldRenderer::update_areas(&mut self, ctx)` — ports `WorldRenderer.cpp:153-168`: `for area in get_areas(ctx)`: `mrea = area.get_member(ctx,"mrea")?.read_u32(ctx)`; if `area.get_member(ctx,"isPostConstructed")?.read_bool(ctx)` is true and `!mesh_by_mrea.contains_key(&mrea)` → `if let Some(m) = load_mesh(ctx, area) { mesh_by_mrea.insert(mrea, m); }`; else (not post-constructed) → `mesh_by_mrea.remove(&mrea)` **and** `gpu_mesh_by_mrea.remove(&mrea)`. (`mrea` / `isPostConstructed` member names: grep `CGameArea.bs`.)
    8. `WorldRenderer::render(&mut self, device, queue, encoder)` — ports the GPU half of `WorldRenderer.cpp:336-406` (minus `renderEntities`):
       - Sync GPU meshes: for each `mrea` in `mesh_by_mrea` not in `gpu_mesh_by_mrea`, create a `DynamicMesh` (Triangles) and `upload(device, queue, mesh.verts)`; drop `gpu_mesh_by_mrea` entries whose `mrea` is gone. Push each collision mesh's AABB lines into `render_buff` here (`shapes::generate_cube_lines(mesh.min, mesh.max, (1,1,1,1))`, `set_transform(IDENTITY)`) — do it in `render` so it happens after `update_areas` regardless of ordering, matching `cpp:374-378`.
       - Upload the 2 immediate buffers to the 4 `DynamicMesh`es (`render_buff.tri_verts()` → opaque-tri mesh, `.line_verts()` → opaque-line mesh; same for translucent).
       - `WorldUniforms::from_matrices(Mat4::IDENTITY, cam_view, cam_projection, cam_eye, light_dir.normalize())` → `pipelines.set_uniforms(queue, &u)` (model is identity for every draw this phase — the immediate buffer bakes per-vertex transforms, and collision verts are already world-space; `cpp:339` / `386` / `397`).
       - One render pass into `(color, depth)`: `LoadOp::Clear` (black, `a=1`) + depth clear `1.0`. Draw order (`cpp:352-403`): (a) collision meshes via the mesh pipeline for the current `CullType` — see Watch-for; (b) opaque `render_buff` tris via `mesh_opaque` (cull Back — `cpp:382-383`) then `render_buff` lines via `line_opaque`; (c) translucent tris via `mesh_translucent` then lines via `line_translucent` (`cpp:393-403`). `DynamicMesh::draw` is a no-op when empty, so unconditional draws are fine.
    9. `src/app.rs`: replace `scene: SpikeScene` / `scene_texture` handling with `world: WorldRenderer` (keep the identical egui user-texture register/update pattern and the `"World"` `egui::Window` + one-frame-lag resize). In `window_event` `RedrawRequested`, inside `if self.defs_loaded` after `get_all_objects`, call `window.world.update(&ctx, &WorldInput::default(), (w,h))` where `(w,h)` is last frame's world-panel pixel size (reuse the existing `world_view_size_pts` → px math; seed with the target's current size on frame 1). `AppWindow::render` calls `self.world.render(&self.device, &self.queue, &mut encoder)`.
    10. Delete `src/scene.rs`; remove `mod scene;` from `main.rs`.
    11. Unit tests in `renderer.rs` (no GPU — split the camera math into a pure free fn or `#[cfg(test)]`-reachable helper): the 3 camera modes produce the expected `cam_view` / `cam_projection` for hand-chosen inputs; `quat_from_euler` matches glm's `quat(vec3)` formula for a couple of angle triples; `update_areas` add-then-evict against a synthetic `mesh_by_mrea` (can test the map bookkeeping with a stub if `load_mesh` needs a real snapshot — otherwise gate on `mem1.raw` like `collision_mesh.rs`).
    12. `cargo build` + `cargo clippy --all-targets` + `cargo test` + `cargo fmt` clean.
  - **Port from:**
    - `../primewatch2/src/world/WorldRenderer.hpp:19-113` — the 3 enums, `GameCamera`, and every `WorldRenderer` camera/light field with its default.
    - `../primewatch2/src/world/WorldRenderer.cpp:115-118:init` — construct the two `ImmediateModeBuffer`s.
    - `../primewatch2/src/world/WorldRenderer.cpp:120-151:update` — input accumulate + clamp; player transform/velocity/lastNonCollidingState.translation/lookDir/orientation/morphState reads off `g_stateManager["player"]`; camera-manager `curCameraId` → `getObjectByEntityID` → retype `CGameCamera` → `game_cam` fields.
    - `../primewatch2/src/world/WorldRenderer.cpp:153-168:updateAreas` — `getAreas` loop, `mrea` key, `isPostConstructed` gate, insert/erase `mesh_by_mrea`.
    - `../primewatch2/src/world/WorldRenderer.cpp:248-310:render` (camera-setup block only) — 3-mode projection/view, orbit-origin z-nudge, `decompose` → `cam_eye`.
    - `../primewatch2/src/world/WorldRenderer.cpp:321-334` + `352-406` — the non-entity CPU geometry (ghost cube, camera lines, per-mesh AABB lines) and the GL draw sequence (uniform poke, depth/cull state, `mesh_by_mrea` draw loop, opaque then translucent immediate-buffer draws). **Skip** `drawPlayer` (`312-318`), `renderEntities` (`336`), `renderImGui` (`408-529`).
    - `../primewatch2/src/PrimeWatchInput.hpp` — `WorldInput` fields.
    - Rust: `src/scene.rs` (the target-creation + resize + egui-composite pattern to fold in and then delete), `src/gl/{shapes,immediate,mesh,shader}.rs` (all the building blocks, currently dead), `src/mem/math_utils.rs` (P8.4.1 `read_as_*`), `src/mem/area_utils.rs::get_areas`, `src/mem/game_object_utils.rs::get_object_by_entity_id`, `src/world/collision_mesh.rs::load_mesh`.
  - **Watch for:**
    - **glm `quat(vec3 euler)` order.** `glm::quat(glm::vec3(0,pitch,yaw))` uses glm's half-angle formula (`w = cx·cy·cz + sx·sy·sz`, `x = sx·cy·cz − cx·sy·sz`, `y = cx·sy·cz + sx·cy·sz`, `z = cx·cy·sz − sx·sy·cz` with `x`=pitch,`y`=yaw,`z`=roll). Find the matching `glam::Quat::from_euler(EulerRot::…)` (or build it from the formula) and unit-test it against hand-computed values — a wrong order silently mis-aims the follow-cam.
    - **fov degrees/radians.** Port `glm::perspective(fov, …)` verbatim (pass `self.fov` as radians). C++'s `fov{45}` default into a radians param looks like a latent bug; do **not** convert — flag it for the human in the manual checklist.
    - **Culling → pipeline variant.** `WorldPipelines` (P8.2) bakes `cull_mode: None` on all 4 pipelines with a `// P8.4:` marker. Wire `CullType` now: simplest is to extend `WorldPipelines::new` to build the two **mesh** pipelines (opaque + translucent) in all 3 cull variants (`None`/`Back`/`Front`) and select by `self.culling` at draw; lines are never culled. The opaque `render_buff` tri draw is always `Back` (`cpp:382-383`), only the `mesh_by_mrea` collision draw honours `self.culling` (`cpp:357-369`). Keep `front_face: Cw` (already set).
    - **Gamma.** The world shader already applies `linear_to_srgb` to every output (P8.2, per the P1.3 "renderer does its own linear→sRGB" decision, because egui composites the linear `Rgba8Unorm` target without re-encoding). Leave it as-is; add "world view is not double-gamma'd" to the manual checklist.
    - **`Option`-not-total in `update`.** Unlike `load_mesh`'s bulk reads, the player/camera reads here should **keep the last good value** on a `None` (mid-load), not substitute 0 — a zeroed transform would snap the camera to the origin every load.
    - `get_object_by_entity_id` returns `Option`; a `None` camera lookup (early boot) must leave `game_cam` untouched, not panic.
    - Member-name drift: confirm `transform` / `velocity` / `orientation` on `CActor`/`CPhysicsActor`, `translation` on `CMotionState`, and `mrea` / `isPostConstructed` on `CGameArea` against the real `.bs` before wiring — the C++ names may not be the schema names.
    - `app.rs` currently seeds `SpikeScene::new(&device, (800,600))` and drives size from the `"World"` panel with a documented one-frame lag — preserve that exactly for `WorldRenderer`.
    - MSAA stays single-sample (matches deleted `scene.rs` + the `WorldPipelines` `// TODO(P8.4): MSAA` — leave the TODO, don't implement).
  - **Done when:**
    - `cargo build` / `cargo clippy --all-targets` clean — **no new** warnings; the `gl::{shapes,immediate,mesh,shader}` + `math_utils` dead-code warnings should mostly clear as `WorldRenderer` consumes them (baseline was 98).
    - `cargo test` + `cargo fmt --check` pass; new camera-math + euler-quat + `update_areas`-bookkeeping tests included.
    - `SpikeScene` is gone (`src/scene.rs` deleted, `mod scene;` removed); `WorldRenderer` compiles into `app.rs` and the `"World"` egui window renders the world target.
    - **Manual checklist (needs a display — pending human, like P1.2/P1.3):**
      - [ ] With `mem1.raw` (or live Dolphin) loaded, the World view shows room collision geometry (grey/green/tinted tris with dark edge lines), white AABB wireframe boxes around loaded areas, and the coloured camera-frustum line set.
      - [ ] The translucent pink "last known non-colliding" box renders at the player position.
      - [ ] Colours look right — the world view is **not** double-gamma'd (too dark/washed) vs the egui chrome.
      - [ ] `CameraMode::GameCam` matches the in-game camera; `FollowPlayer` frames the player (note: fov may be wrong pending the degrees/radians decision above).
      - [ ] Camera orbit/zoom is inert until P9.1 wires winit input — expected.
      - [ ] **fov decision (reviewer/human):** `perspective()` passes `self.fov` (default `45`) to `glam …::perspective` **unconverted** — verbatim port of the C++ `glm::perspective(fov, …)`, whose first arg is radians. `45` rad is almost certainly a latent C++ bug. Left as-is per the Watch-for; decide in a later P8.4 pass or P9 whether to `to_radians()` it.

  - **Implementation notes (P8.4.2 — port-implementer):**
    - New `src/world/renderer.rs` (`pub mod renderer;` in `src/world/mod.rs`). `src/scene.rs` deleted, `mod scene;` removed from `main.rs`.
    - Enums `CullType` / `CameraMode` / `OrbitPlayerCameraOrigin` + `GameCamera` + `WorldInput` ported from `WorldRenderer.hpp` / `PrimeWatchInput.hpp`. The three enums carry a justified `#[allow(dead_code)]` (only the default variant is constructed until the P8.4.6 UI); `GameCamera` / `WorldRenderer` `pub` fields not yet read (player_pos/velocity/orientation/look_vec, game_cam fov/znear/zfar/aspect) do **not** warn (pub fields of a constructed pub struct) — commented `// P8.4.3:` / `// P8.4.5:`.
    - **Schema member names — all confirmed against the real `.bs`, no substitutions:** `CActor.transform`, `CPhysicsActor.{velocity,orientation}`, `CMotionState.translation`, `CPlayer.{lookDir,morphState}`, `CGameArea.{mrea,isPostConstructed}`, `CGameCamera.{perspectiveMatrix,fov,znear,zfar,aspect}` (+ inherited `transform`), `CCameraManager.curCameraId`, `CStateManager.{player,cameraManager}`.
    - **fov:** ported verbatim (no deg→rad) — `NOTE:` in `perspective()` + manual-checklist item above.
    - **`quat_from_euler`:** implemented straight from glm's half-angle formula (`w=cx·cy·cz+sx·sy·sz`, …), called as `quat_from_euler(vec3(0, pitch, yaw))`. Unit-tested against the hand-computed formula for three angle triples + a yaw-only Z-rotation sanity check.
    - **`glm::decompose` → `cam_eye = cam_view.inverse().w_axis.truncate()`** for all 3 modes (only `cam_eye` is consumed this phase, as the shader `viewPos`); `camPointing` / `camViewport` deferred to P8.4.5.
    - **Keep-last-value** on any `None` player/camera read (not zero). `update_areas` also `continue`s (does not evict) on a `None` `mrea` / `isPostConstructed` read — conservative vs the C++ total read; a valid snapshot never misses.
    - **`CullType` → pipelines:** `src/gl/shader.rs` `WorldPipelines` now builds the two **mesh** pipelines in all 3 `CULL_MODES` (`[None, Back, Front]`); `WorldPipelines::mesh(translucent, cull)` selects one. Lines never culled; opaque + translucent `render_buff` tris always `Back` (C++ `cpp:382-383` inherited state); only the `mesh_by_mrea` collision draw honours `self.culling`. Removed the now-unused `bind_group_layout` field.
    - Camera math split into pure `compute_camera(&CameraParams) -> CameraResult` and `reconcile_area(...)` (map bookkeeping) free fns for GPU-free unit tests. 9 new tests in `renderer.rs` (3 camera modes, euler-quat ×2, orbit z-nudge, `reconcile_area` add/evict + load-failure).
    - `app.rs`: `SpikeScene`→`WorldRenderer` (`world` / `world_texture` / new `world_view_px`); identical egui register/update-texture + `"World"` window + one-frame-lag resize pattern preserved. `update` called in `RedrawRequested` after `get_all_objects`, `render` from `AppWindow::render`.
    - **Commands:** `cargo build` → clean, 44 warnings (baseline 98, all pre-existing dead-code in not-yet-wired P5/P7/P8.3 modules + bstruct); `cargo clippy --all-targets` → clean, no warnings in `renderer.rs` / `app.rs` / `shader.rs`; `cargo test` → `82 passed; 0 failed`; `cargo fmt --check` → clean.
    - Not run: anything needing a display (the manual checklist stays pending a human, like P1.2/P1.3).

  - **Review (port-reviewer):** Verdict DONE. Walked `WorldRenderer.{hpp,cpp}:19-113 / 115-168 / 248-406` side by side with `src/world/renderer.rs`, `src/gl/shader.rs`, `src/app.rs`.
    - Enums / `GameCamera` / all camera+light field defaults match `WorldRenderer.hpp:83-113`.
    - Input accumulate + clamps match `cpp:123-128` (`pitch +-(PI/2-0.1)`, `distance [1,100]`).
    - All schema member names verified against the real `.bs`: `CActor.transform 0x34`, `CPhysicsActor.{velocity 0x138, lastNonCollidingState 0x1F4, orientation 0x200}`, `CMotionState.translation 0x0`, `CPlayer.{morphState 0x2F8 (enum, read as u32), lookDir 0x500}`, `CGameArea.{mrea 0x84, isPostConstructed 0xF0:7:1}` (bitfield - `read_bool` routes through the P4.2 bit mask), `CGameCamera.{perspectiveMatrix 0xEC, znear 0x160, zfar 0x164, aspect 0x168, fov 0x184}` + inherited `transform`, `CCameraManager.curCameraId 0x0` (i16, read via `read_u16` per C++).
    - Keep-last-value on every `None` player/camera read confirmed; `update_areas` continues (no evict) on a `None` structural read.
    - `quat_from_euler` = glm half-angle formula verbatim, called `(0,pitch,yaw)`; tests pin the literal formula + a yaw-only Z-rotation.
    - `fov` passed to `perspective()` unconverted - faithful port of `glm::perspective(fov, ...)`; `NOTE:` in code + manual-checklist `fov decision` item present. Acceptable.
    - Culling: only the `mesh_by_mrea` collision draw honours `self.culling`; opaque + translucent `render_buff` tri draws always `Back` (`cpp:382-383`); lines never culled. `WorldPipelines` builds the 2 mesh pipelines in all 3 `CULL_MODES`; `front_face: Cw` preserved; dead `bind_group_layout` field dropped.
    - `glm::decompose` -> `cam_eye = cam_view.inverse().w_axis.truncate()` per the task spec. Minor behavioural deviation: glm `decompose` yields the view-matrix translation column (`-R*eye`) whereas `view.inverse().w_axis` is the true world eye position. Only consumed as the shader `viewPos` (specular) this phase - sanctioned by the task; revisit in P8.4.5 if `camPointing`/`camViewport` need the raw decompose.
    - Gamma: shader `linear_to_srgb` untouched (P8.2/P1.3 decision); manual-checklist item present.
    - `src/scene.rs` deleted, `mod scene;` gone from `main.rs`, no dangling refs; `app.rs` preserves the exact egui user-texture register/update + one-frame-lag `"World"` panel resize pattern (now also stashing `world_view_px` for next-frame `update`).
    - `cargo fmt --check` clean; `cargo test` 82 passed / 0 failed; `cargo clippy --all-targets` no errors, no new warnings (remaining are pre-existing dead-code in not-yet-wired P5/P7/P8.3 modules + `bstruct` submodule lifetime lints).
    - **Manual-verification (needs a display / live Dolphin) still pending a human** - the P8.4.2 checklist above, in particular the `fov` degrees/radians decision and "world view not double-gamma'd".

