# P4.2 — archived completed task

- Task id: **P4.2**
- Status: archived completed task
- Commit: recorded in the commit that adds this file (`port(P4.2): ...`)
- Summary lives in [`TASKS.md`](../TASKS.md); this file is the full historical entry.

**Review (port-reviewer):** Verified `GameInstance` typed reads against C++ `GameMember::read_*`
(`GameDefinitions.cpp:246-283`) and `getBits` (`:234-243`). Integer reads route through the
P3.1 `GameMemory::read_u{8,16,32,64}_bits` helpers (no re-ported masking); `read_f32/f64/string`
apply no bit masking, matching C++; `read_bool` = `read_u8 != 0` with mask. The `Option<i64>`→`u32`
clamp (`unwrap_or(0).max(0)`) is done once at the `GameInstance` boundary via `bit_u32`/`bit_length_u32`.
`get_member` carries `member.bit`/`bit_length` into the resolved instance; `GameInstance::new` and its
`globals.rs` callers are unchanged. The Option-return / no-default-substitution deviation from the P3.1
forward-note is an orchestrator decision, documented in Step 4 and the doc-comment; coherent (P7
callsites default). `cargo fmt --check` clean; `cargo test` 12 passed / 0 failed (mem1.raw present,
parity tests ran). Pre-existing `collapsible_if` lints in P4.1 `get_member_by_name`/`extends` left
untouched (out of scope) — optional future cleanup. No unrelated edits.

---

- [ ] **P4.2** Add typed reads on `GameInstance`: `read_u8/u16/u64/f32/f64/bool/string` + bitfield
  masking (`bit` / `bit_length`). — `IN REVIEW`

  **Implementation notes (port-implementer):**
  - `src/structs/prime_structs.rs`: `GameInstance` gains `bit: Option<i64>` / `bit_length:
    Option<i64>` (both `None` for `new`, so `globals.rs` roots unchanged). Added
    `GameInstance::with_bitfield(address, type_name, bit, bit_length)` and private `bit_u32()` /
    `bit_length_u32()` helpers doing the `Option<i64>`→`u32` unwrap (`.unwrap_or(0).max(0) as u32`)
    at the boundary. `get_member` now returns `with_bitfield(addr, type_name, member.bit,
    member.bit_length)` — pointer auto-deref path unchanged.
  - New typed reads on `GameInstance` (`&self, mem: &GameMemory) -> Option<T>`): `read_u8/u16/u64`
    added, `read_u32` reworked — all four route through `GameMemory::read_u{8,16,32,64}_bits` with
    the unwrapped bit/length. `read_bool` = `read_u8(mem).map(|v| v != 0)`. `read_f32/f64` →
    `mem.read_f32/f64(self.address)`, `read_string` → `mem.read_string(self.address)` — no bit
    masking, matches C++. No `read_i*` added (out of parity scope).
  - Deviation recorded per Step 4: reads return `Option` with no default substitution (overrides the
    P3.1 forward-note); defaulting deferred to P7 callsites.
  - Tests (skip-if-absent on `../primewatch2/mem1.raw`): `game_instance_reads_match_raw_memory`
    (raw parity for every read), `game_instance_bitfield_masking` (bit slices vs
    `read_u32_bits`, negative bit/length clamp, f32 ignores bits), `game_instance_oob_reads_are_none`,
    and `get_member_carries_bitfield` (in-memory schema, no dump). All 12 crate tests pass.
  - `cargo fmt` clean. `cargo clippy --all-targets`: no new lints vs the P4.1 baseline — only the
    pre-existing dead-code warnings on the still-unused `GameInstance`/`GameMember` API (baseline
    diffed via `git stash`) and two pre-existing `collapsible_if` warnings in P4.1 code (left alone,
    out of scope). `cargo test`: `ok. 12 passed; 0 failed`.

  **Steps:**
  1. [x] `src/structs/prime_structs.rs` — add `bit: Option<i64>` and `bit_length: Option<i64>` fields to
     `GameInstance`. Keep `GameInstance::new(address, type_name)` as-is (both default to `None` — its
     callers in `globals.rs` are struct roots, never bitfields); add a `with_bitfield(address,
     type_name, bit, bit_length)` ctor or set the fields directly in `get_member`.
  2. [x] In `GameInstance::get_member`, carry `member.bit` / `member.bit_length` into the returned
     `GameInstance` so a typed read on the result can mask. (Pointer auto-deref stays as-is; a
     pointer member's bit fields are still carried but will normally be `None`.)
  3. [x] Add typed reads on `GameInstance`, mirroring the C++ `GameMember::read_*` surface exactly
     (`GameDefinitions.cpp:246-283`): `read_u8`, `read_u16`, `read_u64`, `read_f32`, `read_f64`,
     `read_bool`, `read_string`. `read_u32` already exists — rework it to apply the same bit masking
     for consistency. Each takes `&self, mem: &GameMemory` and returns `Option<T>`.
     - `read_u8/u16/u32/u64`: route through the existing `GameMemory::read_u{8,16,32,64}_bits(addr,
       bit, bit_length)` helpers. Unwrap `self.bit` / `self.bit_length` (`Option<i64>` → `u32`) at
       this boundary: `self.bit.unwrap_or(0).max(0) as u32` etc. — C++ `getBits` uses
       `value_or(0)`; `bit_length == 0` already means "whole value" in `extract_bits`.
     - `read_bool`: `read_u8(mem).map(|v| v != 0)` — matches C++ `read_u8() != 0` (mask applies).
     - `read_f32/f64`: `mem.read_f32(self.address)` / `read_f64` — **no** bit masking (C++
       `read_f32` calls `GameMemory::read_float` directly, no `getBits`).
     - `read_string`: `mem.read_string(self.address)` — no masking.
  4. [x] Deviation to record: C++ reads are total (`getRealPtr` clamps OOB to 0, so callers get a plain
     value). The Rust ports return `Option<T>` and do **not** substitute a default — this overrides
     the P3.1 forward-note ("P4.2 GameInstance owns default substitution when a read is None"):
     defaulting is deferred to the P7 render callsites (`.unwrap_or_default()` there), keeping these
     reads composable with `?` and letting the inspector distinguish "unreadable" from "zero". The
     `bit`/`bit_length` `Option<i64>` → `u32` unwrap *is* done here, as that note specified.
  5. [x] Unit tests in `prime_structs.rs`, reading `../primewatch2/mem1.raw` via
     `GameMemory::load_from_file` (skip-if-absent, same pattern as `game_memory.rs` tests). Build a
     `GameInstance` at a known address and assert `read_u32` matches the raw `GameMemory::read_u32`,
     a bitfield slice (construct a `GameInstance` with `bit`/`bit_length` set) matches
     `GameMemory::read_u32_bits`, `read_string` at `0x8000_0000` == `"GM8E01"`, and an OOB address
     returns `None`. A schema-driven `get_member(...).read_*` round-trip is nice-to-have if an
     unambiguous static field exists, but the address-level tests are the requirement.

  **Port from:**
  - `../primewatch2/src/defs/GameDefinitions.cpp:234-243:getBits` — `bit`/`length` default to 0;
    `length == 0` ⇒ mask = all-ones; result `(v >> bit) & mask`. Already ported as
    `GameMemory::extract_bits` (P3.1) with shift-UB guards — reuse via the `_bits` helpers, don't
    re-port.
  - `../primewatch2/src/defs/GameDefinitions.cpp:246-283:GameMember::read_*` — `read_bool` =
    `read_u8() != 0`; `read_u8/u16/u32/u64` = `getBits(GameMemory::read_uN(offset), bit, bitLength)`;
    `read_f32/f64` = raw `GameMemory::read_float/double(offset)` (no getBits); `read_string` =
    byte loop, 255 cap, NUL-terminated (already ported as `GameMemory::read_string`).
  - Note the C++ `offset` is the fully-resolved absolute game address (post member-chain + pointer
    deref); the Rust equivalent is `GameInstance::address`, already resolved by `get_member`.

  **Watch for:**
  - `.rustfmt.toml` = 2-space indent. `cargo clippy --all-targets` + `cargo test` clean.
  - Don't add `read_i*` variants — not in the C++ `GameMember` surface (P4 scope is parity).
  - `read_u32`'s signature already returns `Option<u32>` and takes `&GameMemory` — keep it; just add
    masking. Existing callers: none outside tests (`area_utils.rs` is commented out).
  - No `Ctx` yet (that's P4.5) — keep the explicit `mem: &GameMemory` parameter.
  - Negative `bit` / `bit_length` from a malformed `.bs` shouldn't panic — clamp with `.max(0)`.

  **Done when:** the 7 new typed reads + reworked `read_u32` exist with correct bit-mask routing and
  the `Option<i64>`→`u32` unwrap at the `GameInstance` boundary; `GameInstance` carries `bit` /
  `bit_length` populated by `get_member`; tests prove raw-read parity, bitfield masking, string
  read, and OOB `None`; clippy + test green.
