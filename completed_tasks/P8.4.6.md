# P8.4.6 — archived completed task

- Task id: **P8.4.6**
- Status: `DONE`
- Commit: the commit that adds this file (`port(P8.4.6): …`).
- Summary lives in [`TASKS.md`](../TASKS.md); this file is the full historical entry.

---

- [x] **P8.4.6** Visibility-toggle UI: `TriggerRenderConfig` / `ActorRenderConfig` / `CameraMode` egui widgets. — `DONE`

  **Scope:** port the render-config half of `PrimeWatch::doMainMenu` — the **Culling**, **Camera**,
  **Triggers**, **Actors** menus plus the **Camera Controls** window — as egui, driving `WorldRenderer`
  state that already exists (P8.4.2/P8.4.3). The **Attach** menu (pids / detach / file dialog) and the
  **Tools** menu (Reload Definitions, Raw Data View, Raw Demo View, exact-values toggle) touch
  `MemoryAccess` / the inspector, not `WorldRenderer` — they are **P9.1**; leave a `// P9.1:` marker
  where they'd slot into the menu bar.

  **Port from:**
  - `../primewatch2/src/PrimeWatch.cpp:383-393:doMainMenu` — **Culling** menu: three mutually-exclusive
    items mapping to `CullType::{BACK,FRONT,NONE}`. Note the C++ label/value skew ("Show Front" sets
    `BACK`, "Show Back" sets `FRONT`) — port the labels *and* the mapping verbatim.
  - `../primewatch2/src/PrimeWatch.cpp:396-433:doMainMenu` — **Camera** menu: `cameraMode` ∈
    `{FOLLOW_PLAYER, GAME_CAM, DETATCHED}`; when `FOLLOW_PLAYER`, an indented Top/Center/Bottom group
    bound to `orbitPlayerCameraOrigin`; when `DETATCHED`, a `manualCameraSpeed` slider (0.1..2.0,
    `AlwaysClamp`) + a "Show camera controls" toggle bound to `showExactCameraControls`.
  - `../primewatch2/src/PrimeWatch.cpp:435-464:doMainMenu` — `TOGGLE_MENU` macro + the **Triggers**
    (14 bools, separator before Water/Docks) and **Actors** (6 bools) menus. Field-name order is the
    struct-declaration order in `../primewatch2/src/world/WorldRenderer.hpp:46-71`.
  - `../primewatch2/src/PrimeWatch.cpp:322-336:doFrame` — **Camera Controls** window (shown while
    `showExactCameraControls`): `camLineLength` drag (2..250), `manualCameraPos` drag3, and Yaw/Pitch
    drags that display **degrees** (`glm::degrees`, yaw `fmod 360`) and write back **radians**
    (`glm::radians`); pitch clamp -89..89, yaw -360..360.
  - `../primewatch2/src/world/WorldRenderer.hpp:91:manualCameraSpeed` — `float manualCameraSpeed{1.0f}`,
    not yet on the Rust `WorldRenderer`; add it.

  **Steps:**
  - [x] 1. `src/world/renderer.rs`: add `pub manual_camera_speed: f32` (default `1.0`, ports hpp:91) and
     `pub show_exact_camera_controls: bool` (default `false`, ports C++ `PrimeWatch::showExactCameraControls`
     — a comment noting it's app-shell state parked on `WorldRenderer` for now) to the struct + `new`.
  - [x] 2. `WorldRenderer::render_menu(&mut self, ui: &mut egui::Ui)` — the four `ui.menu_button(...)` blocks.
     Mutually-exclusive selections via `ui.selectable_value(&mut self.field, Variant, label)`; the
     `TOGGLE_MENU` items via `ui.checkbox(&mut self.trigger_render_config.foo, "label")`. Reproduce the
     conditional sub-sections (FollowPlayer→orbit origin with `ui.indent`; Detached→`ui.add(egui::Slider::new(&mut self.manual_camera_speed, 0.1..=2.0).clamping(egui::SliderClamping::Always))`
     + a `checkbox` for `show_exact_camera_controls`) and the `ui.separator()` before Water/Docks.
  - [x] 3. `WorldRenderer::render_camera_controls(&mut self, ui: &mut egui::Ui)` — ports `doFrame:322-336`.
     Keep the deg↔rad conversion exact (`f32::to_degrees` / `to_radians`, `yaw_deg = self.yaw.to_degrees().rem_euclid(360.0)`
     — verify `fmod` vs `rem_euclid` sign behavior and match C++ `fmod` if they differ; C++ `fmod` keeps
     the sign of the dividend, so use `%`). Only write `self.yaw`/`self.pitch` back when the drag
     `.changed()`.
  - [x] 4. `src/app.rs::render`: behind `if defs_loaded`, add `egui::TopBottomPanel::top("menu_bar").show(&self.egui_ctx, |ui| { egui::MenuBar::new().ui(ui, |ui| { self.world.render_menu(ui); /* P9.1: Attach + Tools menus */ }); });`
     placed *before* the other windows. After the panel, `if self.world.show_exact_camera_controls { egui::Window::new("Camera Controls").resizable(false).show(&self.egui_ctx, |ui| self.world.render_camera_controls(ui)); }`.
     Borrow-checker: `self.world` is borrowed mutably inside the closure while `self.egui_ctx` is
     borrowed — same split already used for `render_status_windows`; clone the `egui::Context` handle
     (`let egui_ctx = self.egui_ctx.clone();`) if the borrow checker complains.
  - [x] 5. Tests (`#[cfg(test)]` in `renderer.rs`): use `egui::__run_test_ui(|ui| { r.render_menu(ui); })`
     on a headless `WorldRenderer`-shaped value to prove the closures type-check and don't panic; assert
     a `selectable_value` write path flips the field (drive it by pre-setting the field and checking a
     pure mapping helper if you factor one out). Keep it light — this is egui glue, not logic.
  - [x] 6. `cargo fmt` (2-space), `cargo clippy --all-targets`, `cargo test` all clean.

  **Watch for:**
  - `WorldRenderer::new` currently takes `(device, size)` and builds GPU state — the test can't call it
    without a `wgpu::Device`. Factor the menu/camera-controls methods so they only touch plain fields,
    and if a test needs an instance, gate it behind a `wgpu` headless device helper *or* extract a
    `#[derive(Default)]` config sub-struct. Prefer a small pure `apply_*` helper you can unit-test over
    forcing a GPU device in tests.
  - Label/value skew in the Culling menu is intentional — do not "fix" it.
  - Field name is `use_collision_impulses` in Rust vs `useCollisionImpluses` (sic) in C++ — the menu
    label should match the C++ label string (`"useCollisionImpluses"`) for parity, or use the corrected
    spelling consistently; pick corrected spelling for the label and note the deviation.
  - egui 0.36: `egui::MenuBar::new().ui(ui, ...)` (not the old `egui::menu::bar`). `ui.menu_button`
    exists. `egui::SliderClamping::Always`.
  - Don't wire Attach/Tools/pids/file-dialog — that's P9.1 and pulls in `DolphinMemoryAccess` plumbing
    the app shell doesn't have yet.

  **Done when:** the four render-config menus + Camera Controls window render from `app.rs` behind the
  `defs_loaded` gate, mutate `WorldRenderer` state, and match the C++ labels/mappings; P9.1 markers
  left for Attach/Tools; `clippy --all-targets` + `test` clean; manual "menu toggles change what draws"
  check deferred to the P8.4.x manual checklist (no display here).

  **Implementation notes (port-implementer):**
  - `src/world/renderer.rs`: added `pub manual_camera_speed: f32` (1.0) and `pub show_exact_camera_controls: bool`
    (false) to `WorldRenderer` + `new`. Removed the now-obsolete `#[allow(dead_code)]` on `CullType` /
    `CameraMode` / `OrbitPlayerCameraOrigin` (all variants are constructed by the new menu).
  - Menu/camera-controls bodies are free `pub(crate)` fns `render_menu_bar(...)` / `render_camera_controls_ui(...)`
    taking `&mut` field refs; `WorldRenderer::render_menu` / `render_camera_controls` are thin forwarders.
    This keeps the UI logic testable via `egui::__run_test_ui` with no `wgpu::Device` (per Watch-for).
    `render_menu_bar` carries `#[allow(clippy::too_many_arguments)]` with a justification comment (it mirrors
    the flat `worldRenderer.*` field set the C++ menu pokes).
  - Culling label/value skew preserved verbatim ("Show Front" -> `Back`, "Show Back" -> `Front`). Camera-mode
    label "Detatched" kept as the C++ misspelling. FollowPlayer -> indented Top/Center/Bottom; Detached ->
    indented Speed `Slider(0.1..=2.0)` w/ `SliderClamping::Always` + "Show camera controls" checkbox. Triggers
    menu: 12 checkboxes + `separator` + Water/Docks, struct-declaration order. Actors menu: 6 checkboxes
    (`renderCollisionActors` deliberately not exposed, matching C++).
  - **Deviation (label):** the trigger checkbox for `use_collision_impulses` is labelled `"useCollisionImpulses"`
    (corrected spelling) not the C++ `"useCollisionImpluses"` (sic) — per Watch-for "pick corrected spelling".
  - **Deviation (egui container):** the task specced `egui::TopBottomPanel::top(...).show(ctx, ...)`, but egui
    0.36 removed the context-level panel API — `Panel` only mounts inside a `Ui`, and this app drives egui via
    `begin_pass`/`end_pass` with no root `Ui` (all existing UI is `Area`/`Window` at ctx level). Used a
    top-anchored `egui::Area` + `egui::Frame::menu(ui.style())` wrapping `egui::MenuBar::new().ui(...)` instead.
    Same "menu bar at the top" UX; consistent with the app's current layout model. `// P9.1:` markers left
    inside the MenuBar closure for the Attach + Tools menus.
  - Camera Controls window: `egui::Window::new("Camera Controls").resizable(false)`; `cam_line_length` /
    `manual_camera_pos` xyz `DragValue`s; Yaw/Pitch shown in degrees (`f32::to_degrees`, `yaw_deg = deg % 360.0`
    — Rust `%` matches C++ `fmod` dividend-sign behavior), written back in radians only on `.changed()`, ranges
    `-360..=360` / `-89..=89`.
  - Tests added in `renderer.rs`: `render_menu_bar_type_checks_and_does_not_panic` (FollowPlayer + Detached
    paths via `__run_test_ui`), `render_camera_controls_ui_type_checks_and_does_not_panic`,
    `culling_menu_label_value_skew_is_preserved`, `camera_controls_yaw_pitch_deg_rad_roundtrip`.
  - `cargo clippy --all-targets`: clean (no errors; pre-existing dead-code / `collapsible_if` / bstruct
    lifetime warnings only — none from this change).
  - `cargo test`: `test result: ok. 106 passed; 0 failed; 0 ignored`.
  - Reviewer: the `Area`-vs-`Panel` deviation is the main thing to sanity-check; also confirm the borrow split
    (`let egui_ctx = self.egui_ctx.clone();` then `self.world` mutated inside the closure) reads OK.

  **Review (port-reviewer):** Menus verified line-by-line against `PrimeWatch.cpp:322-336` / `383-464` and
  `WorldRenderer.hpp:19-93` — Culling label/value skew, Camera sub-groups (orbit origin between "Follow
  Player" and "Game Cam"; Detached speed slider + toggle), Triggers 12+separator+Water/Docks in
  struct-declaration order, Actors 6 (renderCollisionActors omitted), and the Camera Controls deg→rad
  write-on-`.changed()` all match. `TopBottomPanel::top(...).show(ctx, …)` genuinely does not exist in
  egui 0.36 (panels are now the unified `Panel` struct with `show(&mut Ui, …)` only) — the `Area` +
  `Frame::menu` deviation is correct and consistent with the app's ctx-level `Area`/`Window` model.
  `%` matches C++ `fmod` dividend sign. `cargo fmt --check` clean; `cargo test` 106 passed; clippy output
  trusted (untouched handoff tree). Nits (non-blocking, defer to P9.1): Camera Controls window has no
  `.open(&mut …)` close-button binding (C++ passes `&showExactCameraControls`); the `menu_bar` and
  `world-status-host` Areas are both `fixed_pos (0,0)` and will visually overlap until P9.1 lays out the
  shell.

  **Manual verification (needs a display / live Dolphin — pending human, rolls into the P8.4.x checklist):**
  - [ ] Menu bar renders at the top; Culling / Camera / Triggers / Actors open and their items toggle.
  - [ ] Selecting a Culling item visibly changes collision-mesh face culling.
  - [ ] Camera → Follow Player shows Top/Center/Bottom; Detached shows the Speed slider + "Show camera
        controls"; toggling the latter opens/closes the Camera Controls window.
  - [ ] Camera Controls Yaw/Pitch drags read out in degrees and move the detached camera correctly;
        yaw wraps at ±360, pitch clamps at ±89.
  - [ ] Trigger / Actor checkboxes change which entities draw in the world view.
