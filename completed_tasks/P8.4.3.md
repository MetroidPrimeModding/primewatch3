# P8.4.3 — archived completed task

- Task id: **P8.4.3**
- Status: archived completed task
- Commit: the commit that adds this file (`port(P8.4.3): …`)
- Summary / forward-facing notes: see [`TASKS.md`](../TASKS.md)

---

- [x] **P8.4.3** `renderEntities` dispatch + player/trigger/dock/actor/physicsActor draw fns (geometry only). — `DONE`

  **Port from:**
  - `WorldRenderer.cpp:583-662` `renderEntities` — sets `renderBuff` transform to identity; assembles `triggerRenderFlags` (u32 bitmask) from `triggerRenderConfig` (the `detectProjectiles` bit expands to `0x4|0x8|0x10|0x20|0x100|0x200|0x400`); iterates `entities`, `continue` on `!entity["active"].read_bool()`, `isHighlighted = highlighted.contains(entity["uniqueID"].read_u16())`; dispatch chain **in this exact order** via `extendsClass`: `CScriptTrigger` (→ if also `CScriptWater`: draw iff `triggerRenderConfig.water`; else draw iff `entity["triggerFlags"].read_u32() & triggerRenderFlags`), `CScriptDock` (`.docks`), `CGameProjectile` (`.renderProjectiles`), `CBomb` (`.renderProjectiles`), `CPowerBomb` (`.renderProjectiles`), `CPlayer` (no-op — handled by drawPlayer), `CChozoGhost` (`.renderAI`), `CScriptPickup` (`.renderPickups`), `CCollisionActor` (`.renderCollisionActors`), `CAi` (`.renderAI`), `CPhysicsActor` (`.renderPhysicsActors`), `CActor` (`.renderActors`).
  - `WorldRenderer.cpp:531-581` `drawPlayer(ghost, color)` — `buf = color.a < 0.99 ? translucentRenderBuff : renderBuff`; if `ghost.isMorphed`: `buf.setTransform(translate(pos+(0,0,0.7)) * toMat4(orientation))` + `addTris(generateSphere({0,0,0}, 0.7, color))`; else `buf.setTransform(translate(pos))` + `addTris(generateCube({-0.5,-0.5,0},{0.5,0.5,2.7}, color))`. Then speed indicator **always on `renderBuff`**: transform = `translate(pos + (0,0, isMorphed?0.7:1.35))`; `forward = normalize((orientation*(0,1,0)).xy)`, `movement = normalize(velocity.xy)`, `angle = acos(dot(forward,movement)/(len*len))`; speedColor ladder (`|angle|>π/2 || isnan` → red `{1,0,0,1}`; else `percent=angle/(π/2)`, `{0, percent*0.5+0.5, 0, 1}`, and `percent>0.95` → `{0,1,1,1}`); `setColor({1,1,1,1}); addLine({0,0,0},{forward,0}); setColor(speedColor); addLine({0,0,0}, velocity*0.3)`.
  - `WorldRenderer.cpp:664-684` `drawTrigger` — `min/max = readAsCVector3f(entity["bounds"]["min"/"max"])`, `transform = readAsCTransform(entity["transform"])`; color `{1,1,1,0.5}`, `CScriptWater` → `{0.5,0.5,1,0.5}`, highlighted → `{1,0,0,0.5}`; `translucentRenderBuff.setTransform(transform)` + `addTris(generateCube(min,max,color))`.
  - `WorldRenderer.cpp:686-702` `drawDock` — `min/max = entity["collisionPrimitive"]["aabb"]["min"/"max"]` (inherited from `CPhysicsActor`), `transform = entity["transform"]`; color `{0.5,1,0.5,0.5}`, highlighted → red; same translucent cube.
  - `WorldRenderer.cpp:704-739` `drawPhysicsActor` — `transform`, `pos = transform[3]` (col 3 xyz); `min/max = pos + entity["collisionPrimitive"]["aabb"]["min"/"max"]`; if `len(min-max) < 0.1` → `min/max = pos + entity["baseBoundingBox"]["min"/"max"]`; if still `< 0.1` → `min/max = entity["renderBounds"]["min"/"max"]` (**no `pos` offset — preserve the asymmetry**); color white/red-if-highlighted; `translucentRenderBuff.setColor(color); setTransform(identity); addTris(generateCube(min,max,color)); setTransform(transform);` + 3 axis `addLine`s (`{0,∓0.5,0}`).
  - `WorldRenderer.cpp:741-773` `drawActor` — `model = entity["modelData"]` (pointer member `*CModelData`); if `model` resolves to address 0 **and** `!isHighlighted` **and** `!actorRenderConfig.renderAllActors` → return; `min/max = entity["renderBounds"]["min"/"max"]`, `transform`; color white/red; identity-transform cube + transform-space 3 axis lines (same shape as drawPhysicsActor minus the `pos` math).
  - `WorldRenderer.hpp:46-79` — `TriggerRenderConfig` / `ActorRenderConfig` field defaults; `PlayerGhost { enabled=false, position, orientation, velocity, isMorphed=false }`.
  - `WorldRenderer.cpp:312-318,336` — `render()` order: `drawPlayer(player,{1,1,1,1})`; `for ghost in playerGhosts { if ghost.enabled drawPlayer(ghost,{0,1,1,0.5}) }`; `renderEntities(entities, highlightedEids)`. In this port those calls go at the **end of `update`**, after the existing ghost-cube / camera-line accumulation block (`renderer.rs:519-541`).

  **Steps:**
  - [x] 1. `renderer.rs`: add `PlayerGhost` (Default derive), `TriggerRenderConfig`, `ActorRenderConfig` (plain `bool` fields, `Default` impl with the C++ initializers — not real bitfields). Add `WorldRenderer` fields `player: PlayerGhost`, `player_ghosts: [PlayerGhost; 5]`, `trigger_render_config: TriggerRenderConfig`, `actor_render_config: ActorRenderConfig`. Migrate the loose `player_pos` / `player_velocity` / `player_orientation` / `player_is_morphed` fields + their `update` reads into `self.player.{position,velocity,orientation,is_morphed}` (keep the last-good-value-on-`None` semantics); repoint `CameraParams.player_is_morphed` and any other refs. Keep `player_look_vec` / `last_known_non_colliding_pos` as separate fields (they are separate in C++).
  - [x] 2. Change `WorldRenderer::update` signature to also take `objects: &HashMap<TUniqueID, GameInstance>` and `highlighted: &std::collections::HashSet<u16>`. Update the `app.rs` call site to pass `&self.objects` and an empty highlighted set (`// TODO(P9.2): real highlight state`). Import `TUniqueID` from `crate::mem::game_object_utils`.
  - [x] 3. Implement `draw_player(&mut self, ghost: &PlayerGhost, color: Vec4)` per the spec above. Use `shapes::generate_sphere` / `generate_cube`. `orientation * Vec3::Y` then `.truncate()` for the vec2; `Quat::from_mat4`-free — morphed model matrix is `Mat4::from_translation(pos + (0,0,0.7)) * Mat4::from_quat(orientation)`.
  - [x] 4. Implement `draw_trigger` / `draw_dock` / `draw_physics_actor` / `draw_actor` as `&mut self` methods taking `(ctx: &Ctx, entity: &GameInstance, is_highlighted: bool)`. Add a small chained-read helper (e.g. `fn read_vec3_at(ctx, inst, path: &[&str]) -> Option<Vec3>` walking `get_member`) or nest `get_member`; on any `None` in a draw fn, **return early without drawing** (P8.4.2 `None`→skip convention).
  - [x] 5. Implement `render_entities(&mut self, ctx: &Ctx, objects: &HashMap<..>, highlighted: &HashSet<u16>)` — the flag bitmask + full dispatch chain. Add **no-op stubs** for the P8.4.4 fns (`draw_projectile`, `draw_bomb`, `draw_power_bomb`, `draw_chozo_ghost`, `draw_pickup`, `draw_collision_actor`, `draw_ai`) — empty bodies with a `// P8.4.4` marker — so the dispatch structure lands complete now.
  - [x] 6. Call `render_entities(...)` then `draw_player(&self.player, Vec4::ONE)` + the `player_ghosts` loop at the end of `update` (clone `self.player` / ghosts or restructure borrows as needed — the draw fns take `&mut self`).
  - [x] 7. Unit tests (pure, no GPU): `triggerRenderConfig` → bitmask assembly; `draw_trigger` color selection (water vs highlight vs default) factored into a pure helper; `draw_physics_actor` degenerate-bbox fallback selection as a pure helper. Reuse the in-memory `GameStructs` test-builder pattern from `math_utils.rs` if an integration-style test is cheap; otherwise keep helpers pure.
  - [x] 8. `cargo fmt` (2-space), `cargo clippy --all-targets`, `cargo test` all clean. Paste command output into Implementation notes.

  **Implementation notes (port-implementer):**
  - `src/world/renderer.rs`: added `PlayerGhost` / `TriggerRenderConfig` / `ActorRenderConfig` (plain `bool`s + hand-written `Default` matching the C++ bitfield initializers). `WorldRenderer` gained `trigger_render_config` / `actor_render_config` / `player: PlayerGhost` / `player_ghosts: [PlayerGhost; 5]`; the loose `player_pos` / `player_velocity` / `player_orientation` / `player_is_morphed` fields were folded into `self.player.*` and the `update` reads + `CameraParams.player_is_morphed` repointed. `player_look_vec` / `last_known_non_colliding_pos` kept separate (as in C++).
  - `update` signature now `(&mut self, ctx, input, viewport_size, objects: &HashMap<TUniqueID, GameInstance>, highlighted: &HashSet<u16>)`. The C++ `render()` calls (`drawPlayer(player)`, ghost loop, `renderEntities`) are appended to the end of `update` per the plan — after the existing ghost-cube / camera-line block, before `update` returns. No `render()` / GPU changes.
  - Ported `renderEntities` (full `extends_class` dispatch chain, verbatim order), `drawPlayer` (buffer selection by `color.w < 0.99`; speed indicator always on `render_buff`), `drawTrigger`, `drawDock`, `drawPhysicsActor`, `drawActor`. Seven P8.4.4 fns are `&mut self` no-op stubs wired into the chain.
  - Pure helpers (all `pub(crate)`, unit-tested): `trigger_render_flags`, `trigger_color`, `is_degenerate_bbox`, `physics_actor_bbox`, `player_speed_color`. Chained-read helpers `walk_member` / `read_vec3_at`.
  - `#[allow(clippy::collapsible_if)]` on `render_entities` with a justifying comment: collapsing `if extends_class(X) { if config { .. } }` into `&&` would let a class-matched-but-flag-off entity fall through to a base-class branch (e.g. a `CScriptDock` with `docks` off would then hit the `CPhysicsActor` branch). This is the one sanctioned suppression.
  - Deviations: `drawPhysicsActor` reads all three bbox sources (collisionPrimitive / baseBoundingBox / renderBounds) eagerly and skips the actor on any `None` (P8.4.2 `None`→skip), where C++ reads the fallbacks lazily; on a valid snapshot every `CPhysicsActor` has all three so behaviour matches. `Mat4::from_quat` is fed the raw (possibly un-normalized) player quat, verbatim like `glm::toMat4` — glam only asserts normalization under the `glam_assert` feature, which is off.
  - `CScriptWater` confirmed present in `prime_defs` (`CScriptWater: CScriptTrigger`).
  - `app.rs`: call site passes `&self.objects` + an empty `HashSet<u16>` (`// TODO(P9.2): real highlight state`).
  - `cargo fmt`: clean. `cargo clippy --all-targets`: no errors, no new warnings (only pre-existing dead-code in `collision_mesh` / `inspector` / `globals` / `bstruct`, and the pre-existing `collapsible_if` in `prime_structs`). `cargo test`: `test result: ok. 89 passed; 0 failed` (was 82; +7 new pure-logic tests).
  - Note: `cargo clippy --fix` was accidentally run and touched `collision_mesh.rs` / `prime_structs.rs` with pre-existing-lint autofixes; both files were reverted with `git checkout` — they are NOT part of this task.

  **Watch for:**
  - All accumulation is CPU into the two immediate buffers; **no `render()` / GPU changes** beyond what exists. `update` already `clear()`s both buffers once at the top — do **not** clear again in `render_entities`.
  - `renderBuff->setTransform(identity)` at the top of `renderEntities`; every draw fn sets its own transform on `translucentRenderBuff` before `addTris`. Keep the buffer-transform discipline exact or geometry smears.
  - `color.a < 0.99` selects the translucent buffer — the white player cube (alpha 1.0) goes to `render_buff`; trigger/dock/actor cubes (alpha 0.5) to `translucent_render_buff`.
  - `drawPhysicsActor`: `pos` is added to the `collisionPrimitive`/`baseBoundingBox` bounds but **not** to the `renderBounds` fallback. Degeneracy test is `(min-max).length().abs() < 0.1`.
  - `drawActor` `modelData` is `*CModelData` — `get_member` auto-derefs; a null pointer → resolved `address == 0` (or `None`). Treat both as "no model".
  - Dispatch order is load-bearing: `CCollisionActor` before `CAi`, `CAi` before `CPhysicsActor`, `CPhysicsActor` before `CActor`. Port verbatim.
  - `player_ghosts` are all `enabled=false` with nothing populating them yet — the loop is a no-op but port it (matches C++).
  - `extends_class(ctx, "CScriptWater")` — verify `CScriptWater` exists in `prime_defs`; if not, the water branch is dead but keep it (`extends_class` returns false safely).

  **Done when:** `cargo clippy --all-targets` + `cargo test` clean; `renderEntities` dispatch chain + `drawPlayer` / `drawTrigger` / `drawDock` / `drawPhysicsActor` / `drawActor` ported with the seven P8.4.4 draw fns stubbed; `update` accepts `objects` + `highlighted` and `app.rs` passes them; new pure-logic unit tests pass.

  **Review (port-reviewer):** Dispatch chain order + `triggerRenderFlags` mask (incl. `detectProjectiles` 7-bit fan-out and the `CScriptTrigger`->`CScriptWater` special case) verified bit-for-bit against `WorldRenderer.cpp:583-662`. `drawPlayer` / `drawTrigger` / `drawDock` / `drawPhysicsActor` / `drawActor` geometry, colours, buffer selection (`color.w < 0.99`), buffer-transform discipline, and the `renderBounds`-fallback-without-`pos` asymmetry all match C++. Schema members (`bounds`, `collisionPrimitive.aabb`, `baseBoundingBox`, `renderBounds`, `modelData`, `triggerFlags`, `transform`, `active` bitfield, `uniqueID`) confirmed present in `prime_defs/prime1`. `player`/`player_ghosts` migration leaves `compute_camera` + the P8.4.2 tests green. `app.rs` call site passes `&self.objects` + empty highlight set. Working tree is exactly the 3 intended files; the pre-existing `collapsible_if` / `unneeded late initialization` lints in `prime_structs.rs` / `collision_mesh.rs` are untouched. `cargo fmt --check` clean; `cargo test` 89 passed; `cargo clippy --all-targets` no new warnings. Render-order deviation (entities before player, both appended after the camera-line/ghost-cube block) is plan-directed and render-equivalent for depth-tested CPU accumulation. Manual verification (needs display + live Dolphin): on-screen entity boxes/lines, player cube/sphere + speed indicator, trigger/dock/actor colours.
