# P6.2 — archived completed task

Archived completed task. Detail lives here; `TASKS.md` keeps only the forward-looking summary.

- Commit: the commit that adds this file (`port(P6.2): …`).
- Summary + forward-dependencies: see `TASKS.md` Phase 6.

---

- [x] **P6.2** Rewrite `area_utils.rs` — port `AreaUtils::getAreas`. — `DONE`

  **Port from:**
  - `../primewatch2/src/utils/AreaUtils.cpp:8-27:getAreas` — `g_stateManager` → `memberByName("world")`
    (bail `{}` if absent) → `memberByName("areas")` (bail `{}` if absent). `areas` is
    `rstl::vector<rstl::autoptr<CGameArea>>` (`CWorld.bs:7`, offset 0x18). Read `areas["end"]` as
    `u32` — despite the name it is the **element count** (`rstl.bs` `rstl::vector<T> { u32 end; u32
    size; *T first }`). Take `areas["first"]` (the `*T first` member, pointer auto-deref → a handle of
    type `rstl::autoptr<CGameArea>`). `sizePer = structByName(first.type)->size` = `rstl::autoptr<T>`
    size 0x8 (`rstl.bs`). For `i in 0..end`: clone `first`, `offset += sizePer * i`, take
    `["value"]` (the `*CGameArea value 0x4` member of `rstl::autoptr`, pointer auto-deref → a
    `CGameArea` handle), set its display name to `"area {i}"`, push. Return the vec.
  - Consumers (`WorldRenderer.cpp:154,421`, not in scope this phase) only ever index the result and
    read members off each area (`mrea`, `curChain`, `phase`, `isPostConstructed`, `postConstructed`…);
    none read the `.name` — it is purely an inspector label.

  **Steps:**
  1. [x] Replace the entire commented-out body of `src/mem/area_utils.rs` with a real impl. Module doc
     comment: "Ports `../primewatch2/src/utils/AreaUtils.cpp`."
  2. [x] `pub fn get_areas(ctx: &Ctx) -> Vec<GameInstance>`:
     - `let sm = crate::mem::globals::get_state_manager();`
     - `let Some(world) = sm.get_member(ctx, "world") else { return vec![] };`
     - `let Some(areas) = world.get_member(ctx, "areas") else { return vec![] };`
     - `let Some(end) = areas.get_member(ctx, "end").and_then(|e| e.read_u32(ctx)) else { return vec![] };`
     - `let Some(first) = areas.get_member(ctx, "first") else { return vec![] };`
     - loop `for i in 0..end.min(AREA_CAP)` → `let item = first.element(ctx, i);`
       `if let Some(area) = item.get_member(ctx, "value") { result.push(area); }`
     - return `result`.
  3. [x] `element` stride: `first.element(ctx, i)` must use `element_size` = the monomorphized
     `rstl::autoptr<CGameArea>` struct size (0x8), **not** the `primitive_size` fallback (4). Add a
     test asserting `first.element_size(ctx) == 8`.
  4. [x] Tests (`#[cfg(test)]`, same skip-if-absent `mem1.raw` + real `prime_defs` loader idiom as
     `game_object_utils.rs` / `globals.rs`):
     - with the dump: `get_areas` returns a non-empty vec; every area's `address` masks into RAM
       (`addr & 0x8000_0000 == 0x8000_0000`) and `ctx.mem.read_u32(addr).is_some()`; each area's
       `type_name == "CGameArea"`; `first.element_size(ctx) == 8`.
     - zeroed memory: `get_areas` does not panic (world pointer derefs to 0, `end` reads 0 or the
       loop is bounded by `AREA_CAP`).

  **Watch for:**
  - `GameInstance` has **no `name` field** (P6.1 precedent — none was added). Drop the C++
     `area.name = "area {i}"`; the ordered `Vec` index *is* the label, and formatting it is a P7
     render-layer concern. Flag this as the one sanctioned deviation.
  - C++ `getAreas` has **no failsafe** on `end` (unlike `getAllObjects`' `count > size` break). A
     garbage `end` from an unloaded/partway world would spin the frame. Add a private
     `const AREA_CAP: u32` (suggest 1024, mirroring the `getAllObjects` 1024 clamp) and bound the
     loop by it. Flag as a sanctioned defensive deviation.
  - `world` / `areas` are pointer members (`*CWorld`, and `areas` is a value member but `first`
     inside it is `*T`) — rely on `get_member`'s existing auto-deref; don't hand-deref.
  - Confirm the monomorphized struct names resolve: `world.get_member(ctx,"areas")` yields
     `type_name == "rstl::vector<rstl::autoptr<CGameArea>>"` and that key exists in
     `ctx.structs`; `areas.get_member(ctx,"first")` yields `"rstl::autoptr<CGameArea>"`. If a name
     mismatch breaks resolution, that is a bstruct-monomorphization finding — report it, don't
     hardcode a workaround.
  - `Option` reads, no default substitution (P4.2/P5.1 convention). `rustfmt` 2-space.
  - `area_utils` is not yet wired into `App` (its consumer is P8 `WorldRenderer`); leaving
     `get_areas` as dead code with an expected warning is fine — do **not** add a speculative
     per-frame call in `app.rs`.

  **Done when:** `cargo clippy --all-targets` + `cargo test` clean (no *new* warnings beyond the
  expected `get_areas` dead-code one; the pre-existing `area_utils.rs` unused-import warnings are
  gone since the file is rewritten); `get_areas` ported with the tests above; `area_utils.rs` no
  longer contains the stale commented-out block.

  **Implementation notes:**
  - Rewrote `src/mem/area_utils.rs` end-to-end: module doc, `const AREA_CAP: u32 = 1024`,
    `pub fn get_areas(ctx: &Ctx) -> Vec<GameInstance>` following the step-2 skeleton verbatim, and a
    `#[cfg(test)]` module with the three required tests (skip-if-absent `mem1.raw` + real
    `prime_defs` loader idiom copied from `game_object_utils.rs`). Stale commented-out block and its
    now-unused imports are gone.
  - Monomorphization resolves cleanly: `world.get_member(ctx,"areas")` →
    `rstl::vector<rstl::autoptr<CGameArea>>`, `areas.get_member(ctx,"first")` →
    `rstl::autoptr<CGameArea>` (struct size 0x8), `item.get_member(ctx,"value")` → `CGameArea`. No
    bstruct-monomorphization finding; no hardcoded workaround. The live-dump test
    (`get_areas_reads_the_live_world`) passes against `../primewatch2/mem1.raw` — non-empty vec,
    every area `type_name == "CGameArea"`, `address & 0x8000_0000 == 0x8000_0000`, readable.
  - Sanctioned deviations, both documented in the fn doc comment: (1) no `name` field on
    `GameInstance` — dropped the C++ `area.name = "area {i}"`; the `Vec` index is the label. (2)
    `AREA_CAP` bounds the loop (`for i in 0..end.min(AREA_CAP)`) — C++ has no failsafe on `end`.
  - Also (per the P4.2/P5.1 Option convention, same as P6.1): a mid-walk `None` on any structural /
    value read bails early rather than fabricating 0. C++ reads are total. Never triggers on a valid
    snapshot.
  - Expected dead-code warnings: `get_areas` AND `AREA_CAP` (the latter is transitively dead because
    its only use is inside `get_areas`). Both clear when P8 `WorldRenderer` wires the call site — did
    not add `#[allow(dead_code)]` (matches the P6.1 `get_object_by_entity_id` precedent) and did not
    add a speculative `app.rs` call.
  - `cargo fmt` clean. `cargo clippy --all-targets`: clean — no new warnings beyond the two
    dead-code ones above; the pre-existing `collapsible_if` lints in `prime_structs.rs` (P4.1) and
    the workspace `bstruct` / bevy-era warnings are untouched. `cargo test`: 30 passed, 0 failed
    (was 27; +3 from this task).

  **Review:** Verified `get_areas` against `AreaUtils.cpp:8-27` step by step — traversal `g_stateManager`→`world`→`areas`→`end`(count)→`first`→`element(i)`→`["value"]` matches; stride is the monomorphized `rstl::autoptr<CGameArea>` size 0x8 (test-asserted, not the `primitive_size` 4 fallback); both deviations (dropped `name`, `AREA_CAP` bound) are sanctioned and documented; `Option` reads, no default substitution. `cargo fmt --check` clean, `cargo test` 30 passed, clippy adds only the two expected `get_areas` / `AREA_CAP` dead-code warnings. Phase 6 complete.
